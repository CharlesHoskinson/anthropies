# Task 4 QA auditor note (humanize print-prompt + origin blocklist)

Date: 2026-08-17
Role: QA
Task: Humanize print-prompt + origin blocklist (Family 1)
Verdict: APPROVE
Diff: `55e669b..6c3f73f` (`7781e0a` feat + `6c3f73f` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| TDD evidence (RED then GREEN) | PASS |
| `origin_blocklist` exists and matches brief | PASS |
| `humanize_print_prompt_default` returns prompt, not a rewrite | PASS |
| `OriginBlocked` → exit 2 | PASS |
| Input bytes unchanged (no dest / `.cleaned` / `.bak`) | PASS |
| `rewrite_metric.status = not-run` | PASS |

## TDD

RED documented as missing `../src/services/humanizer.js` on both named files. Brief Step 2 expected `originBlocked` / `Humanizer` not defined. Observed (module missing). GREEN: 13 tests / 7 files after implementation. Feat commit `7781e0a` matches brief Step 7. Weak RED (missing module, not a failing assertion) is brief-compliant, same as Tasks 2–3.

## Surfaces

- `tests/origin-blocklist.test.ts` is the brief `origin_blocklist` verbatim: `claude`/`gemini-2.5`/`synthid-demo` true; `ollama`+`llama3.2` and `print-prompt` false. Second test: `ANTHROPIES_REWRITE_MODEL=claude-opus` → `OriginBlocked`, in-place bytes identical, no `.cleaned`, no `.bak`.
- `originBlocked` (`src/services/humanizer.ts:85-89`) lowercases both strings and matches `claude|anthropic|gemini|google-gemini|synthid`.
- `humanizeFile` refuses before `loadOwned` / `writeAtomic` (`humanizer.ts:180-183`). `rewrite()` checks again (`:150-152`).
- CLI maps `OriginBlocked` to exit 2 via `failTags` (`src/cli.ts:40-48,179-181`). tapError prints `OriginBlocked: origin model would re-stamp the mark` on stderr. No `Reporter.print` on the fail path → stdout empty.
- `tests/humanize-print-prompt.test.ts` `humanize_print_prompt_default`: `Humanizer.humanize` prose contains the original sentence and `/Rewrite the text below/`; note `/print-prompt/`; `metric.status === "not-run"`; `surviving_ratio` null; honesty `/best-effort/`; no `/watermark removed/i`; `report.rewrite_metric.status === "not-run"`.
- Print-prompt dest is prefix + Layer-A-cleaned text, not a rewrite. `notRunMetric` only. `computeRewriteMetric` is unused by this path (correct).
- `Humanizer` `R` is `FileSystem`. No `HttpClient` in `src/` or `tests/`. ollama / openai-compatible fall back to print-prompt with an honest note.

## Findings

BLOCKER: none

MAJOR: none on Task 4 file list.

NIT: CLI exit 2 is teardown wiring, not a `Command.exitCode` subprocess assert. Bytes-unchanged is service-level. Same class as Task 3 PNG exit 2.

NIT: typed backend Config cannot carry `claude`. `ANTHROPIES_REWRITE_BACKEND=claude` dies as Config, not `OriginBlocked`. Spec §8 backends are `print-prompt` / `ollama` / `openai-compatible`. Model string is the live block path.

## Not done (correct)

HTTP rewrite backends. `computeRewriteMetric` on print-prompt (`status` must stay `not-run`). `capture` / `demo` stay stubs. Python `src/anthropies/` remains.
