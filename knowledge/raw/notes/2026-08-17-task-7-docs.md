# Task 7 docs auditor note (capture, demo, delete Python)

Date: 2026-08-17
Role: docs auditor
Task: Capture, demo, delete Python (Family 6)
Diff: cdec59b..4863f80 (`1cc6a52`, `4863f80`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 2 NIT.

## Locked surfaces

- README product sentence is the only allowed one-liner. Honesty box is the first section after the hero. Three denials present. Command is `capture`, not `sample`. Version stated `0.2.0`. How-to-run uses `npx anthropies` / `node dist/cli.js` / `pnpm install -g`. Four-channel matrix present. Deferred non-quotation `undetectable` rephrased to detector floor.
- Skill YAML has the post-C2PA trigger plus `Does not claim official watermark removal.` No `strip a Claude watermark`. No destamp. Procedure is `npx anthropies` / `node dist/cli.js`.
- Plugin description is the locked Wave-1 sentence. Version `0.2.0`.
- Slash still: deterministic clean first; do not rewrite with Claude; do not claim official-detector failure.
- `docs/CLAIMS.md` now says skill/README call `npx`. Plugin lock still includes “(once implemented)” and CHANNELS records that.
- CLI help stays spec §13 / CLAIMS sense. `capture` “Does not watermark.” `demo` “Never claims official text-kill.”

## Grep

`rg` over README, `skills/purge-anthropies/SKILL.md`, `commands/purge-anthropies.md`, `.claude-plugin/plugin.json`: no `python3 -m anthropies`, no destamp, no official-kill, no `undetectable` as a claim, no `sample` command.

## NIT

- README has a doubled `---` before How the Mark Works and again before the manifesto.
- Plugin still says “(once implemented)” for C2PA. Matches the locked description; CHANNELS already notes it.

## Out of this task

- `docs/MANIFESTO.md:33` still paraphrases `undetectable` outside the Anthropic quotation. Brief said Keep manifesto.
