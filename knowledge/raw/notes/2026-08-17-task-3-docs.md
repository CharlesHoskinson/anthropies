# Task 3 docs auditor note (Family 1 classify + Layer A)

Date: 2026-08-17
Role: docs auditor
Task: Classify + Layer A + text inspect/clean (Family 1)
Diff: 68995de..1975bbf (`fa96737`, `1975bbf`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT.

## Locked surfaces

- `src/report.ts` `honestyStanza` still emits the three required denials. `makeTextReport` always calls it, including after a successful Layer A clean.
- `Reporter.print` writes the stanza to stderr on every inspect/clean; `--json` keeps stdout Schema-only (`json_stdout_purity` rejects `/watermark removed/i`).
- CLI help in `src/cli.ts` keeps spec §13 / CLAIMS.md sense. `inspect` never a single watermark score. `clean` does not remove the keyed text mark.
- JSDoc on new exports is one sentence and capability-true (`classify` is not a verdict; Layer A is deterministic trailers/banners/unicode).
- Named tests: `cert_layer_a_roundtrip`, `binary_guard_docx_png_stdin`, `json_stdout_purity`, `write_guard`. No `removesWatermark`.
- Fixtures under `fixtures/layer-a/`: trailer-claude, banner-generated, zwsp, keep-emoji-zwj, keep-human-trailer. No `sample_watermarked.*`.

## Grep

`rg` over new `src/` `tests/` `fixtures/`: no destamp, no undetectable as a capability, no `watermark removed` as a verdict, no `sample_watermarked`.

## NIT

Spec §13 example is `fixture-c2pa-present.png`. These files omit the `fixture-` prefix. Brief named `fixtures/layer-a/trailer-claude.txt`. Not a claim lie.
