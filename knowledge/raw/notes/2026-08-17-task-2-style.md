# Task 2 STYLE auditor note (Effect scaffold)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Effect scaffold (Family 1 start)
Diff: `a0d919c..94680aa` (`46a51ae` scaffold + `94680aa` wiki ingest)
Scope: tsconfig, Effect 3, Schema.TaggedError Fails, Report, Config, @effect/cli, forbidden imports, 0.2.0

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 2 Step 7)

Style fails on forbidden imports, shared AppR, Fail/Finding mix, or non-`@effect/cli` argv.

| Check | Result | Cite |
|---|---|---|
| Effect 3 not 4 | PASS | `package.json:26` `effect@^3.22.1`; lock `effect@3.22.1`; `@effect/cli@0.77.0`. Caret cannot float to 4. |
| ESM / NodeNext / strict | PASS | `package.json:5` `"type":"module"`; `tsconfig.json:3-10` `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `module`/`moduleResolution` `NodeNext`, `verbatimModuleSyntax`. No dual CJS. |
| Schema.TaggedError Fails | PASS | `src/fail.ts` seven tags: BinaryInput, OriginBlocked, MissingApiKey, PreMarkModel, DecodeError, WriteGuard, InputTooLarge. MissingTool is not a Fail. |
| Fail ≠ Finding | PASS | Fails are TaggedError. Findings are `Schema.Class` on Report. OfficialFinding is TaggedClass, not TaggedError. |
| Report no `suspicious` | PASS | `src/report.ts:62-72`. `removed` nested per channel (`Removed` class). No watermarkScore / destamped / mixed bag. |
| OfficialFinding no score on Unavailable | PASS | `src/report.ts:37-40` empty payload. |
| Config not process.env | PASS | `src/config.ts` Config.option / Config.withDefault / Config.redacted. Unset detect URL is a value. No WATERMARKS_*. |
| @effect/cli | PASS | `src/cli.ts` CliCommand.make + withSubcommands + run + NodeContext.layer + NodeRuntime.runMain. No commander/yargs/minimist. Named CliCommand, not Command. |
| process.argv only at CliCommand.run | PASS | `src/cli.ts:44` `run(process.argv)` — spec §5.4 exception. |
| Forbidden imports | PASS | No node:fs, node:child_process, node:http, fetch, process.env, console.log, Effect.runPromise, default exports, any, as unknown as in src/tests TS. |
| 0.2.0 | PASS | `package.json:3`, `src/cli.ts:42`. |
| Handler stubs | PASS | Finding-free `Console.error` stubs. Brief allows. |
| pnpm-workspace.yaml | PASS | `allowBuilds` only; no `packages:` split. Required for pnpm 11 esbuild postinstall. |
| Shared AppR | N/A | No Effect.Service yet. Scaffold does not invent AppR. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **eslint.config.js:46-64** — spec §5.4 forbids `as unknown as`, but eslint has no selector for it and is not type-aware (`parserOptions.project` unset). Current tree has zero casts. Later families could add `as unknown as` without a red lint.

2. **tsconfig.json:23** — `include` is `src/**/*.ts` only. `tsc --noEmit` does not typecheck `tests/`. Fine for this 17-line claim test; later FileSystem / Layer tests should get a project so `exactOptionalPropertyTypes` applies.

## Not defects

- `pnpm-workspace.yaml` is not a workspace split. User/brief allow it if required to run.
- `src/report.ts:71` optional `official: OfficialFinding` is extra vs spec §8 required list. OfficialFinding still needs a Schema home; findings.status cannot carry `url`/`raw`. Not a mixed score.
- `eslint.config.js` default export is flat-config, not library code.
- Plugin stays `0.1.0`. Python `src/anthropies/` stays. Both later tasks.
- Handlers exit success with a stub message. Brief: stubs OK.

## Conclusion

Effect 3 ESM CLI scaffold matches spec §5 / §8 / §9. STYLE hard gate is green. APPROVE. Do not hold Family 1 text work for the NITs.
