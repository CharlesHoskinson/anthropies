# Wave 3 Task 1 docs auditor note (Family 1 rewrite path)

Date: 2026-08-17
Role: docs auditor (hard gate)
Task: Real ollama / openai-compatible rewrite + computed rewrite_metric
Diff: `cd809a6..d9a9873` (`d15a5c7` feat + `d9a9873` wiki ingest)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 3 NIT (2 out of this family's file list).

## Locked surfaces (Wave 3 spec §2 + CLAIMS)

- `package.json:3`, `.claude-plugin/plugin.json:3`, `src/cli.ts:152` are `0.4.0`. Plugin description is the locked CLAIMS sentence (curly apostrophe). Unchanged except version.
- `src/services/humanizer.ts:113-114` print-prompt note is `print-prompt: does not destamp; run this on an unmarked local model`. Spec §2 required denial. Metric stays `not-run`.
- CLI `humanize` help (`cli.ts:129-131`) still CLAIMS sense: best-effort, refuses Claude and Gemini. `demo` still `Never claims official text-kill` (`cli.ts:143`). Product one-liner unchanged.
- Skill YAML (`skills/purge-anthropies/SKILL.md:3-8`) and slash (`commands/purge-anthropies.md:3`) still deny official watermark removal / official-detector failure. Not this family's files.
- `docs/CLAIMS.md` unchanged. Still forbids destamp-as-claim and official-kill. Spec Family 3 says leave it.

## Grep

Changed copy (`src/cli.ts`, `src/config.ts`, `src/services/humanizer.ts`, plugin, wiki ingest): no `undetectable`, `official detector will fail`, `proves human-written`, `watermark removed`, `official-kill` as a capability. `destamp` appears only in the spec-mandated denial (`does not destamp`) and implementer notes describing that denial.

## NITs

- `README.md:59` — print-prompt section still omits "does not destamp". Family 3 copy.
- Honesty stanza statistical line can stay `not-run` after `rewrite_metric.status=computed` (implementer residual). Report field is correct.
- `destamp` substring in the user-facing note vs CLAIMS forbidden list. Spec §2 requires the denial; same pattern as Wave 1 `does not prove the official Claude text detector will fail`.
