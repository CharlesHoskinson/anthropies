# Wave 3 Task 1 QA auditor note (Family 1 rewrite path)

Date: 2026-08-17
Role: QA
Task: Real rewrite path (ollama + openai-compatible) + computed rewrite_metric
Verdict: APPROVE
Diff: `cd809a6..d9a9873` (`d15a5c7` feat + `d9a9873` wiki ingest)
Scope: fake HTTP tests, rewrite_metric computed vs not-run, origin block, no live model, no official-kill

## Checks (this persona)

| Check | Result |
|---|---|
| Fake HTTP backend in default CI | PASS |
| `rewrite_metric` computed after real rewrite (`n >= 200` prose) | PASS |
| print-prompt stays `not-run` | PASS |
| `n < 200` is `insufficient`, not `computed` | PASS |
| Origin blocklist unchanged; block before POST | PASS |
| No live model in default CI | PASS |
| No official-kill / destamp-as-claim | PASS |
| Ratio is never a pass bar | PASS |

## Independent review

Static review of `cd809a6..d9a9873`. Tests not executed in this auditor environment. Style already APPROVE.

`tests/humanize-rewrite-backend.test.ts` injects `HttpClient.make` + canned JSON. Records POST method/URL. No `node:http`, no `createServer`, no `fetch`. `pnpm test` is `vitest run`; `test:live` is still capture-only (`LIVE=1` + key + allowlist). Default backend remains `print-prompt` (`config.ts`).

`rewrite()` calls `computeRewriteMetric` only after a successful ollama / openai-compatible rewrite. print-prompt returns `notRunMetric`. Tests assert `computed` at 220 tokens, `insufficient` on a short sentence, and print-prompt `not-run` (`tests/humanize-print-prompt.test.ts`). No assertion that surviving_ratio must be under a threshold.

`ORIGIN_TOKENS` still `claude|anthropic|gemini|google-gemini|synthid`. Origin test uses `claude-opus`; `recorded.length === 0`; input bytes unchanged. Non-loopback without `ALLOW_REMOTE=1` is `RewriteRemoteDenied` and also does not POST.

Rewrite note is `rewrote via ${backend} (${model})` and is asserted not to match `/watermark removed/i`. Plugin description still denies defeating the unpublished detector. Official adapter still `Unavailable` unless `ANTHROPIC_DETECT_URL` is set. No MarkLLM. No `src/http/`.

## Surfaces

- `tests/humanize-rewrite-backend.test.ts` — fake POST; computed / insufficient / origin-before-POST / remote deny / ALLOW_REMOTE.
- `tests/humanize-print-prompt.test.ts` — default `not-run`; no removal claim.
- `tests/origin-blocklist.test.ts` — tokens + bytes unchanged (print-prompt path).
- `src/rewrite-backend.ts` — loopback default; ollama `/api/generate`; openai-compatible `/v1/chat/completions`.
- `src/services/humanizer.ts:27` tokens unchanged; `:168` block before POST; `:192` compute after rewrite.
- `src/rewrite-metric.ts` — `computed` iff `n >= 200` and domain is not `code`.

## Findings

BLOCKER: none

MAJOR: none

NIT: `reportFromHumanize` keeps `makeTextReport` honesty `statistical: "not-run"` after a computed `rewrite_metric`. Report field is correct. Spec says surviving 5-grams are reported, never a pass bar. Family 2 copy can align the stanza.

NIT: print-prompt / origin-blocklist tests do not pin `ConfigProvider`. A developer env with `ANTHROPIES_REWRITE_BACKEND=ollama` could leave the default-CI path. Default CI does not set that knob.

## Not done (correct)

No live ollama. No MarkLLM. No official detect client. No `src/http/`. No 5-gram gate. Family 2 copy and Family 3 prompt snapshots are later.
