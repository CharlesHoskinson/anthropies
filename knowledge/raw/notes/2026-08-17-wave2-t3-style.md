# Wave 2 Task 3 STYLE auditor note (Skill HTTP path)

Date: 2026-08-17
Role: STYLE (markdown honesty / no code leaks)
Task: Skill HTTP client path (Wave 2 family 3)
Diff: `0524caa..9c05bb9` (`216e961` docs + `9c05bb9` wiki ingest)
Scope: `skills/purge-anthropies/SKILL.md`, README serve/version copy. No TypeScript in this family.

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 3 / Wave 2 spec §2)

This family is markdown. Style fails on destamp/official-kill, invented HTTP success, HTTP `/humanize`, or leaking `src/` / Effect internals / secrets into the skill.

| Check | Result | Cite |
|---|---|---|
| No destamp / official-kill | PASS | SKILL frontmatter: does not claim official watermark removal. Hard rule + Do-not row: do not claim official-detector failure. Required denial: "This run does not prove the official Claude text detector will fail" (`SKILL.md:68`). No `destamp`, `undetectable`, `watermark score`, `watermark removed`, `strip a Claude watermark`. README honesty box unchanged. |
| Official stays unavailable | PASS | `SKILL.md:68,111` official stays `unavailable` unless `ANTHROPIC_DETECT_URL` is set. README new serve paragraph same. |
| No HTTP `/humanize` | PASS | `SKILL.md:68,75` humanize is CLI-only; no `/humanize` route. README: "There is no HTTP `/humanize`." |
| Health first; do not invent success | PASS | `SKILL.md:36-43` `curl -sS -f` health; if health fails, say so; do not invent a successful HTTP result. Operator-required service → stop. Else `npx anthropies` / `node dist/cli.js` fallback (plan). |
| Clean scope honest | PASS | `/clean` annotated "Layer A / hard-bound metadata only" (`SKILL.md:62`). Opening: hidden-character strip does not touch the keyed wording. Residual: not an official-detector certificate. |
| Capture not sample | PASS | Diff does not call capture a sample or a watermark proof. README existing capture sentence unchanged. |
| Version / loopback | PASS | Health expected `{"ok":true,"version":"0.3.0"}` matches `server.ts` `unsafeJson({ ok: true, version })`. README `0.3.0`. Default `http://127.0.0.1:8765`. Remote bind needs `--host`. |
| No code leaks | PASS | No `src/` paths, no Effect/`HttpRouter`/`Schema`/`catchHttpFail`/temp-dir internals, no TypeScript. Curl payloads are the locked public contract `{ file, name }` → `{ ok, kind, report, cleaned? }`. Env names only; Bearer is `$ANTHROPIES_SERVER_API_KEY`, no secret value. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **`SKILL.md:28`** — "The process that owns FileSystem" is Wave 2 spec §1 wording. It is not a leak of `writeUpload` / scoped temps. Still Effect-platform jargon on a user-facing skill.

2. **`SKILL.md:53-65`** — HTTP `/clean` example stops at the JSON body. Honest that `cleaned` is base64; it does not show decode-to-path. Persist is implicit. Completeness, not a lie.

## Not defects

- Plan `npx` fallback vs spec "do not invent a local fallback that contradicts the operator": skill does both (report health failure; CLI fallback only when the operator did not require the service). Honest.
- POSIX `base64` / `basename` is a host-portability residual (implementer). Not a leak.
- `docs/CLAIMS.md` still has no `serve` row. Out of this task's file list (DOCS / later).
- `commands/purge-anthropies.md` unchanged. Correct scope.
- "owns FileSystem" does not name `src/http/server.ts` or Effect services.

## Conclusion

Family 3 matches Wave 2 spec §2 and the STYLE hard gate for markdown: honesty holds, no official-kill, no invented HTTP success, no `/humanize`, no `src/` leak. APPROVE. Do not hold Task 4 for the NITs.
