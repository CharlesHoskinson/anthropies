# Task 3 STYLE auditor note (classify + Layer A + text inspect/clean)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Classify + Layer A + text inspect/clean (Family 1)
Diff: `68995de..1975bbf` (`fa96737` feat + `1975bbf` wiki ingest)
Scope: Effect.Service, FileSystem-only R, no node:fs, classify/Layer A purity, Fail vs Finding, @effect/cli

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 3

## STYLE gate (plan: Task 3 / spec §5–§6)

Style fails on forbidden imports, shared AppR, Fail/Finding mix, non-pure classify/Layer A, HttpClient in inspect/clean R, or non-`@effect/cli` argv.

| Check | Result | Cite |
|---|---|---|
| Effect.Service + Default | PASS | `Inspector` `src/services/inspector.ts:16`, `Cleaner` `cleaner.ts:22`, `Detector` `detector.ts:18`, `Reporter` `reporter.ts:80`. CLI merges Defaults (`src/cli.ts:115`). No typed AppR. |
| FileSystem-only R | PASS | `Inspector.inspect` `inspector.ts:24` is `Effect<Report, BinaryInput \| DecodeError \| InputTooLarge, FileSystem>`. `Cleaner.clean` `cleaner.ts:30-33` same + `WriteGuard`. No `HttpClient`. |
| classify is pure | PASS | `src/kind.ts:64` `(bytes, suffix?) => Kind`. Not an Effect. Magic before suffix. |
| Layer A is pure | PASS | `src/layer-a.ts:110` `string → LayerAResult`. No Effect import. Effect begins at `loadOwned` / `writeAtomic`. |
| no node:fs | PASS | Reads/writes via `@effect/platform` `FileSystem` (`registry.ts:62-70`, `reporter.ts:93-107`). Tests use platform FS + `NodeContext`. |
| Fail ≠ Finding | PASS | Fails stay `Schema.TaggedError` in `src/fail.ts`. Findings are `Schema.Class` on `Report`. `ResidualHits` (`src/cli.ts:30-32`) is CLI-edge exit 1, not a Fail and not on Report. |
| @effect/cli | PASS | `CliCommand.make` + `Options`/`Args` + `withSubcommands` + `run` + `NodeContext.layer` + `NodeRuntime.runMain`. `process.argv` only at `src/cli.ts:152`. |
| Forbidden imports | PASS | No `node:fs`, `fetch`, `process.env`, `console.log`, `Effect.runPromise`, `any`, `as unknown as`, default exports in library/tests TS. `node:crypto` in `reporter.ts:2` is not forbidden. |
| 0.2.0 / Effect 3 | PASS | `package.json:3,26` `0.2.0` / `effect@^3.22.1`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **spec §5.1 Test layers** — services expose `Default` only. Tests provide Live + `NodeContext.layer`. Fine for Family 1; later families should add `Test` as spec requires.

2. **spec §6 `Match.valueTags`** — dispatch is `Partial<Record<Kind, FormatHandler>>` + `handlerFor` (`src/formats/registry.ts:33-57`). Extensible and fail-closed. Not exhaustive `Match.valueTags`.

3. **tests/json-stdout.test.ts:1** — platform process command imported as `Command`, not spec `ProcCommand`. File does not also import CliCommand, so no collision today.

## Not defects

- `.md` / `.html` / `.svg` fail closed as `BinaryInput` until later families register handlers. `--force-text` proceeds.
- `ResidualHits` is the certificate-channel exit-1 signal. Findings stay on Report.
- `json_stdout_purity` compiles then runs `dist/cli.js` via platform Command (not `node:child_process`).
- Python `src/anthropies/` stays. Official adapter stays `Unavailable`. No HTTP.

## Conclusion

Family 1 matches spec §4–§6 and the Task 3 STYLE hard gate. APPROVE. Do not hold Task 4 for the NITs.
