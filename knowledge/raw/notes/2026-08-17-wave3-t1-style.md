# Wave 3 Task 1 STYLE auditor note (Family 1 rewrite path)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Real ollama / openai-compatible rewrite + computed rewrite_metric
Diff: `cd809a6..d9a9873` (`d15a5c7` feat + `d9a9873` wiki ingest)
Scope: Effect HttpClient, no `node:http`, origin blocklist, loopback default, Fail OriginBlocked

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 3

## STYLE gate (Wave 3 spec §2 + Family 1)

Style fails on `node:http`, missing origin Fail, non-loopback default, or a non-platform HTTP client.

| Check | Result | Cite |
|---|---|---|
| Effect `HttpClient` | PASS | `src/rewrite-backend.ts` uses `@effect/platform/HttpClient` + `HttpClientRequest.post`. CLI `humanize` provides `NodeHttpClient.layer` (`src/cli.ts`). Tests inject `HttpClient.make` canned JSON (`tests/humanize-rewrite-backend.test.ts`). No live model. |
| no `node:http` | PASS | No `node:http` / `node:https` / `createServer` / `fetch` in `src/` or `tests/`. Implementer did not stand up a Node loopback server. |
| Origin blocklist | PASS | Tokens still `claude\|anthropic\|gemini\|google-gemini\|synthid` (`humanizer.ts:27`). `originBlocked` is pure (`humanizer.ts:94-98`). Checked before POST in both `rewrite` and `humanizeFile`. |
| Fail `OriginBlocked` | PASS | Still `Schema.TaggedError` (`fail.ts:10-13`). New `RewriteFailed` / `RewriteRemoteDenied` are Fails too (exit 2 via `failTags` in `cli.ts`). Not Findings. |
| Loopback default | PASS | `rewriteBaseUrl` defaults `http://127.0.0.1:11434` (`config.ts:21-23`). `isLoopbackHostname` is localhost / `::1` / `127.0.0.0/8`. Non-loopback without `ANTHROPIES_REWRITE_ALLOW_REMOTE=1` is `RewriteRemoteDenied` before POST. |
| Config not `process.env` | PASS | `rewriteApiKey` / `rewriteAllowRemote` / `rewriteBaseUrl` are `Config`. Tests use `ConfigProvider.fromMap`. |
| print-prompt default | PASS | Backend default still `print-prompt`. `Humanizer.Default` does not bind `NodeHttpClient`. Metric stays `not-run`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **`src/rewrite-backend.ts:117`** — `completeRewrite` types `R = never` and yields `Effect.serviceOption(HttpClient)`. Missing client is `RewriteFailed` (correct Fail), but the signature hides the requirement. Prefer `R = HttpClient` on `completeRewrite`; keep `serviceOption` only at the Humanizer print-prompt branch so default tests stay client-free.

2. **`src/rewrite-backend.ts:65-70`** — `new URL()` in try/catch instead of `Effect.try` / `Either.try`. Sync and mapped to `RewriteFailed`. Harmless.

3. **`src/rewrite-backend.ts:143-145`** — request body is `bodyUnsafeJson`. Response path already uses Schema. Encode the ollama / openai payloads.

## Not defects

- `serviceOption` so print-prompt tests never construct a client. Matches Wave 1 Task 4 “no HttpClient in default path.”
- Not binding `NodeHttpClient` into `Humanizer.Default`. Per-command `R` on CLI `humanize` is the right edge.
- `::ffff:127.0.0.1` is not treated as loopback — fail-closed. QA, not STYLE.
- Honesty stanza statistical line still `not-run` when `rewrite_metric` is `computed`. Report field is correct. Family 2/3 later.
- No `src/http/`, no MarkLLM, no official-kill.

## Conclusion

Family 1 matches the STYLE hard gate: platform HttpClient, no `node:http`, origin blocklist Fail, loopback default. APPROVE.
