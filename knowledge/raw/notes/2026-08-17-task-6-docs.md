# Task 6 docs auditor note (markup + office + PDF)

Date: 2026-08-17
Role: docs auditor
Task: Markup + office + PDF (Families 3–5)
Diff: e2865eb..5091d86 (`5904842`, `5091d86`)

## Verdict

APPROVE-WITH-CHANGES. 0 BLOCKER. 1 MAJOR. 3 NIT.

## Locked surfaces

- Skill YAML unchanged from Task 5. Still: `Does not claim official watermark removal.` C2PA trigger is only `clean hard-bound C2PA metadata from owned png/jpg/svg (and other supported files).` No `strip a Claude watermark`. No destamp. No official-kill.
- CLI help in `src/cli.ts` unchanged and still locked: inspect never a single watermark score; clean does not remove the keyed text mark; demo never claims official text-kill.
- Fixture is `fixtures/c2pa/fixture-c2pa-present.svg`. `THIRD_PARTY.md` says planted SVG `<metadata>` is synthetic, not a signed Claude Output, not WR bytes.
- Named tests: `cert_c2pa_png_jpeg_svg` (SVG row), `zip_bomb_and_caps`, `cert_markup_html_md`, `cert_office_docx_odt`, `cert_pdf_degraded`. No `removesWatermark`. No `sample_watermarked.*`.
- SVG honesty uses locked `honestyStanza` (three denials) plus soft-binding. Clean residual is `removed` / `hard-bound C2PA/metadata`, not `watermark removed`.
- PDF missing-tool path correctly sets `report.degraded` and Finding `status: degraded`, and appends `warning: exiftool or qpdf missing; PDF metadata not stripped`. It also writes `c2pa: degraded` on the locked honesty line. That verb is not in CLAIMS (`present | absent | removed | not-applicable`).

## Grep

`rg` over new `src/formats/{svg,html,md,docx,odt,pdf,zip,meta-keys}.ts`, `src/services/{inspector,cleaner,reporter,c2pa}.ts`, `skills/purge-anthropies/SKILL.md`, new cert tests, `fixtures/THIRD_PARTY.md`: no destamp, no undetectable as a capability, no official-kill, no `watermark removed` as a verdict, no mixed score.

## MAJOR

PDF inspect/clean (`inspector.ts:138`, `cleaner.ts:148`) pass `c2paHonesty: "degraded"` into `honestyStanza`. Change: keep `c2pa:` as `present` (markers still visible) / `absent` / `removed` / `not-applicable`. Leave `degraded` on Finding + `report.degraded`.

## NIT

- Implementer DOCS self-review listed `degraded` as a c2pa honesty verb.
- HTML/MD AI metadata labeled `c2pa` (CLI “C2PA/metadata,” not a credential).
- Preferred spec test name `removesHardBoundC2paFromPng` unused; forbidden name absent.

## Out of this task

- README `undetectable` remains Task 7.
- Skill procedure still calls `python3 -m anthropies` (CLAIMS until Python delete).
