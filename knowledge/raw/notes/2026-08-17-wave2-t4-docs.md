# Wave 2 Task 4 docs auditor note (Docker + compose)

Date: 2026-08-17
Role: docs auditor
Task: Dockerfile + compose (Wave 2 family 5)
Verdict: APPROVE
Diff: `9c05bb9..1c02563` (`c00f2c8` feat + `1c02563` wiki ingest)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT (`docs/CLAIMS.md` still has no `serve` row; out of this file list).

## Required checks

| Check | Result |
|---|---|
| Product sentence | PASS — README keeps “Restore clean title in Outputs the user already owns.” |
| Version `0.3.0` | PASS — README how-to-run stays `0.3.0`. compose `image: anthropies:0.3.0`. |
| Docker / compose | PASS — `CMD ["node", "dist/cli.js", "serve", "--host", "0.0.0.0", "--port", "8765"]`. compose publishes `127.0.0.1:8765:8765`. README: `docker compose up --build`; in-container `0.0.0.0:8765`; host mapping stays loopback. |
| No heavy backends | PASS — image does not install qpdf / exiftool / c2patool. `/capabilities` probes those binaries; they report absent. README and comments say so. |
| Honesty | PASS — “same service” means inspect/clean HTTP, not full CLI humanize/capture. Official stays unavailable unless `ANTHROPIC_DETECT_URL` is set. Residual is not a certificate. |
| No official-kill | PASS — Dockerfile/compose comments and README docker paragraph do not destamp, do not say undetectable, do not use watermark-removed as a verdict. Official unavailable unless URL. No HTTP `/humanize` (compose comment). |

## Locked surfaces (this task)

- `Dockerfile`: Node 22 bookworm-slim; corepack pnpm 11.22.0; `pnpm build`; runtime `USER node`; `EXPOSE 8765`; comments deny heavy backends.
- `compose.yaml`: `127.0.0.1:8765:8765`; `read_only: true`; `tmpfs: /tmp`; image `anthropies:0.3.0`.
- `.dockerignore`: drops `node_modules`, `.git`, `.worktrees`, `knowledge`, tests, docs, fixtures, skills.
- `README.md`: one Docker paragraph; version still `0.3.0`.

## Grep

Dockerfile, compose.yaml, README docker paragraph: no `destamp`, no `undetectable`, no `watermark removed` as a verdict, no `strip a Claude watermark`. Comments say official stays unavailable unless `ANTHROPIC_DETECT_URL` — locked denial, not a capability.

## NIT

- `docs/CLAIMS.md` CLI help list still has no `serve` row (and no Docker). Task 2/3 already reserved this. File list is Dockerfile + compose + `.dockerignore` + README paragraph.

## Not this task

GitHub CI (Task 5). Raw `docker run -p 8765:8765` LAN publish is not the documented path; README documents compose only.
