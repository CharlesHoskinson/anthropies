# Task 5 STYLE auditor note (raster C2PA)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Raster C2PA (Family 2)
Diff: `5c498c7..59ee369` (`27238d7` feat + `59ee369` wiki ingest)
Scope: Effect.Service C2pa, no node:fs, FileSystem-only R (optional CommandExecutor), Fail≠Finding, no HttpClient, ESM/strict

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 1

## STYLE gate (plan: Task 5 / spec §5 / §6 / §9)

Style fails on forbidden imports, shared AppR, Fail/Finding mix, non-`@effect/cli` argv, C2pa not `Effect.Service`, `node:fs`, HttpClient on the raster default path, or C2pa/Inspector/Cleaner R wider than FileSystem (+ optional CommandExecutor).

| Check | Result | Cite |
|---|---|---|
| C2pa Effect.Service + Default | PASS | `src/services/c2pa.ts:24-65` `Effect.Service` + `accessors: true` + `succeed`. `C2pa.Default` wired from Inspector/Cleaner (`inspector.ts:55`, `cleaner.ts:72`). No typed AppR. |
| FileSystem-only R (optional CommandExecutor) | PASS | Inspector/Cleaner raster methods are `R = FileSystem` (`inspector.ts:26`, `cleaner.ts:32-36`). C2pa inspect/strip are `R = never` (bytes only). `c2patool` is a pure helper (`hasManifestFromToolOutput`, `c2pa.ts:20-21`) and is not on default `R`. |
| no node:fs | PASS | Load/write via platform `FileSystem` (`registry.ts:64-78`, `reporter.ts:127-144`). Tests: `FileSystem` + `NodeContext`. No `node:fs` / `fs/promises`. |
| no HttpClient | PASS | Grep of `src/` and `tests/` TS: no `HttpClient`. Raster path type-checks with HttpClient absent. |
| Fail ≠ Finding | PASS | Truncated raster is `DecodeError` (`c2pa.ts:34-36,55-57`; `fail.ts:27-31`). `c2pa` present/absent is a Finding on `Report` (`reporter.ts:92-97`). Residual is Finding + CLI `ResidualHits` exit 1 (`cli.ts:37-39,62-64,86-88,173-175`) — not a Fail, not in `failTags`. Missing `c2patool` is not a Fail. |
| ESM / strict | PASS | `package.json` `"type": "module"`. `tsconfig.json` `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `NodeNext`, `verbatimModuleSyntax`. Imports use `.js`. |
| Raster parse is pure | PASS | `src/formats/raster.ts` is stdlib/byte parsing. Effect starts at C2pa / read-write. |
| Forbidden imports | PASS | No `node:fs`, `node:child_process`, `fetch`, `process.env`, `console.log`, `Effect.runPromise`, `any`, `as unknown as`, default exports in library/tests TS. `process.argv` only at `CliCommand.run` (`cli.ts:186`). |
| @effect/cli | PASS | Inspect/clean remain `CliCommand.make`. Services merged at `cli.ts:144-149`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **spec §5.1 Test layers** — `C2pa` exposes `Default` only (`succeed`). No `Test` layer. Tests provide Live + `NodeContext.layer` (same residual as Tasks 3–4). Fine for stdlib PNG/JPEG parse.

## Not defects

- C2pa Live `R` is `never`, not the spec-table `FileSystem and/or CommandExecutor`. Spec §5.2 raster inspect/clean is FileSystem-only. Optional native extras must not widen default `R`. Bytes-in parse matches spec §6 (“bytes or a Scope temp”).
- `hasManifestFromToolOutput` is a free function, not a service method. Keeps `CommandExecutor` off Live C2pa.
- Inspector/Cleaner special-case `kind === "raster"` instead of a text `FormatHandler`. Byte family; `handlesKind` documents it (`registry.ts:59-61`).
- Unused `_kind` on C2pa inspect/strip: brief requires the arg; codec is re-sniffed via `rasterCodec`. WebP/AVIF/HEIC stay `absent` until a later parser.
- Tests import platform `Command` (not `node:child_process`). CLI side is `CliCommand`. Not both imported as `Command`.
- Python `src/anthropies/` stays. STYLE gate is TS `src/` + `tests/`.

## Conclusion

Task 5 matches spec §5 / §6 / §9 and the STYLE hard gate: `C2pa` is `Effect.Service`, raster `R` is FileSystem-only, no `node:fs`, no `HttpClient`, Fails stay Fails, Findings stay on `Report`. APPROVE. Do not hold Family 2 for the Test-layer NIT.
