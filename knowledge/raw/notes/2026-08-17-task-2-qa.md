# Task 2 QA auditor note (Effect scaffold)

Date: 2026-08-17
Role: QA
Task: Effect scaffold (Family 1 start)
Verdict: APPROVE
Diff: `a0d919c..94680aa` (`46a51ae` scaffold + `94680aa` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| TDD evidence (RED then GREEN) | PASS |
| `official_claim_forbidden` exists and matches brief | PASS |
| Honesty stanza has both does-not-prove lines | PASS |
| `OfficialFinding.Unavailable` has no `score` | PASS |
| `--help` lists inspect/clean/humanize/capture/demo | PASS (source + `dist/cli.js`) |
| Version `0.2.0` | PASS (`package.json:3`, `cli.ts:42`) |

## TDD

RED documented as `ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND` (no `package.json`). Brief Step 2 expected FAIL (package or module missing). GREEN: one passing test after `src/report.ts`. Single commit at the end matches brief Step 8. Weak RED (missing manifest, not a failing assertion) is brief-compliant.

## Surfaces

- `tests/official-claim-forbidden.test.ts` is the brief test verbatim. Matches `/does not prove the official Claude text detector will fail/` and `/does not prove the text is human-written/`. Rejects `/watermark removed/i` and `/undetectable/i`.
- `src/report.ts:82-90` `honestyStanza` emits those two lines plus the absence-of-mark line.
- `src/report.ts:37-40` `OfficialUnavailable` is `TaggedClass("Unavailable", {})`. No `score` field. `Available` is `url` + `raw` only.
- `src/cli.ts:37-39` `withSubcommands([inspect, clean, humanize, capture, demo])`. Compiled `dist/cli.js:12` same five names. `CliCommand.run(..., { version: "0.2.0" })`.
- `package.json` version `0.2.0`, product sentence, `bin.anthropies` → `./dist/cli.js`.
- `Report` has no `suspicious`. `removed` is nested per channel.

## Findings

BLOCKER: none

MAJOR: none on Task 2 file list.

NIT: Wave named test `official_unavailable_default` (decode fails if a `score` is injected on `Unavailable`) is not in this task. Brief QA gate is the claim test only. Later family.

## Not done (correct)

Handlers are Finding-free stubs. No classify / Layer A. Python and plugin `0.1.0` remain. Effect 4 not installed.
