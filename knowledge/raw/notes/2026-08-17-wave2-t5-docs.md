# Wave 2 Task 5 docs auditor note (GitHub Actions CI)

Date: 2026-08-17
Role: docs auditor
Task: GitHub Actions CI (Wave 2 family 6)
Verdict: APPROVE
Diff: `1c02563..8c5c396` (feat `ci.yml` + implementer wiki ingest)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 2 NIT (`docs/CLAIMS.md` still has no `serve` row; README does not mention CI). Both out of this file list.

## Required checks

| Check | Result |
|---|---|
| Implementer note matches workflow | PASS — triggers `push`/`pull_request` to `main`; matrix `ubuntu-latest` + `windows-latest`; Node 22; pnpm 11.22.0; `pnpm install --frozen-lockfile`, `pnpm test`, `pnpm build`. |
| Wiki ingest | PASS — synthesis `Wave 2 Task 5 Implementer Note` is on the tree and linked from syntheses index, Wave 2 MOC, overview, and log. |
| Product sentence / honesty box | PASS — family did not touch README honesty, manifesto, or legal. Product sentence stays. |
| Version `0.3.0` | PASS — implementer note keeps `0.3.0`. Workflow does not bump or advertise another version. |
| CI surface | PASS — job name `ci (${{ matrix.os }})`. Spec §2: GitHub Actions `pnpm test` + `pnpm build` on push/PR (ubuntu + windows). |
| No `/humanize` | PASS — workflow does not advertise `/humanize`. Implementer: “Almost added HTTP `/humanize`. Did not.” |
| Official detect | PASS — workflow does not set `ANTHROPIC_DETECT_URL`. Official stays unavailable unless that URL is set. |
| No official-kill | PASS — workflow comments/name do not destamp, do not say undetectable, do not use watermark-removed as a verdict. |

## Locked surfaces (this task)

- `.github/workflows/ci.yml`: name `ci`; push/PR `main`; matrix ubuntu + windows; Node 22; pnpm 11.22.0; frozen-lockfile → test → build.
- `knowledge/raw/notes/2026-08-17-wave2-task-5-implementer.md` + `knowledge/wiki/syntheses/Wave 2 Task 5 Implementer Note.md`.

## Grep

`.github/workflows/ci.yml` + implementer note: no `destamp`, no `undetectable`, no `watermark removed` as a verdict, no official-kill claim, no `/humanize`. Denial “official stays unavailable unless `ANTHROPIC_DETECT_URL`” is the locked line, not a capability.

## NIT

- `docs/CLAIMS.md` CLI help list still has no `serve` row. Known residual from Task 2/3/4. File list is just the workflow.
- README does not mention GitHub Actions CI. Not in Task 5 file list. Not a blocker unless the workflow itself claimed destamp / official-kill (it does not).

## Not this task

Docker / compose (Task 4). Full-diff trio before calling Wave 2 done (plan Task 5 last checkbox). Ignore later commit `f4e9828`.
