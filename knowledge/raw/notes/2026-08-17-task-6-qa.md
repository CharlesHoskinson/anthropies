# Task 6 QA auditor note (markup + office + PDF)

Date: 2026-08-17
Role: QA
Task: Markup + office + PDF (Families 3–5)
Verdict: APPROVE
Diff: `e2865eb..5091d86` (`5904842` feat + `5091d86` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| TDD evidence (RED then GREEN) | PASS |
| `cert_c2pa_png_jpeg_svg` SVG row | PASS |
| `zip_bomb_and_caps` | PASS |
| PDF degraded exit 0 | PASS |
| `--json` purity (inspector/cleaner/reporter touched) | PASS |
| No official-detect lie | PASS |

## TDD

RED documented as SVG inspect/clean hitting `BinaryInput: classified as svg` and `tests/zip-caps.test.ts` missing `../src/formats/zip.js`. Stronger than a missing-module-only RED on the SVG row. GREEN: 31 tests / 13 files. Feat commit `5904842` (one commit for all three families; Inspector/Cleaner is one table). PDF cert tests were not in the RED command.

## Surfaces

- `tests/cert-c2pa.test.ts` SVG row of `cert_c2pa_png_jpeg_svg`: inspect planted `<metadata>c2pa planted` → kind `svg`, `c2pa` present, `degraded` false. Clean drops `<metadata>` and `c2pa`; file still has `<svg`. After-inspect `c2pa` absent, `degraded` false. Fixture is synthetic (`THIRD_PARTY.md`).
- `tests/zip-caps.test.ts` `zip_bomb_and_caps`: patched 16-byte cap on a 32-byte member → not ok / zip reason. Claimed-200-MiB CD (not a real bomb) → `Cleaner.clean` `DecodeError`, no `.cleaned` / `.bak`. `unzipCapped` reads CD sizes before `unzipSync`.
- PDF degraded exit 0: `cert_pdf_degraded` locks `residualDrivesExit` false when `degraded: true` even if `c2pa` present. `Cleaner.clean` with missing tools writes dest (`%PDF` bytes), `degraded` true, Finding `c2pa` `degraded`. CLI success path is teardown 0 unless `ResidualHits` (`src/cli.ts:62-64,86-88,165-168`). Spec § table: missing qpdf/exiftool → exit 0.
- `--json`: `Reporter.print` still `Console.error` honesty then `Console.log(JSON.stringify(Schema.encodeUnknownSync(Report)))`. New reports are `Report` + `OfficialUnavailable`. No `console.log` / `HttpClient` / `shell: true` / `node:fs` in new format code. Existing `json_stdout_purity` unchanged.
- Official: `makeContainerReport` always `official: unavailable`, `OfficialUnavailable`, honesty `unavailable (ANTHROPIC_DETECT_URL unset)` plus `this run does not prove the official Claude text detector will fail`. No score field. SVG fixture is not a Claude signature.

## Findings

BLOCKER: none

MAJOR: none on Task 6 named tests.

NIT: PDF exit 0 is `residualDrivesExit` + dest write, not a CLI subprocess. Same class as Task 5 leftover wiring.

NIT: `Layer.provide(missingTools)` on sealed `Cleaner.Default` may not replace Live `PdfTools`. CI without the tools still hits the live degraded path; the unit `residualDrivesExit` lock remains.

NIT: TDD RED is SVG + zip only. `cert_pdf_degraded` appears GREEN-only.

NIT: SVG row does not assert `/soft-binding/`. `makeContainerReport` extraHonesty always appends it for SVG.

NIT: Honesty line `c2pa: degraded` is a CLAIMS verb issue (docs MAJOR). QA contract is Finding `degraded` + `report.degraded` suppressing exit 1. Not residual laundering; spec wants exit 0.

## Not done (correct)

Embedded media / pixel strip. EPUB / XLSX / PPTX. Real zip bomb artifact. Live qpdf/exiftool on default `R`. Python `src/anthropies/` remains.
