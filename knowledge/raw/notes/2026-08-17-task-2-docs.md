# Task 2 docs auditor note (Effect scaffold)

Date: 2026-08-17
Role: docs auditor
Task: Effect scaffold (Task 2)
Diff: a0d919c..94680aa (`46a51ae`, `94680aa`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 0 NIT.

## Locked surfaces

- `package.json:3-4` — version `0.2.0`; description is the product sentence only: `Restore clean title in Outputs the user already owns`.
- `src/cli.ts` — `CliCommand.run` version `0.2.0`; root description is the product sentence; subcommands `inspect | clean | humanize | capture | demo`.
- CLI help is spec §13 / `docs/CLAIMS.md` sense. No watermark-kill claim. `clean` says it does not remove the keyed text mark. `demo` never claims official text-kill. Command is `capture`, not `sample`.
- `NOTICE:9-34` — WR MIT block + reproduced MIT text. Trademark paragraph kept.
- JSDoc on exported schema/Fail/Config/Kind/Report APIs is one sentence and capability-true. `RewriteMetric` is labeled “Not a watermark score.” No banned CLAIMS.md phrases in new TS copy.
- `honestyStanza` emits the three required denials. `official_claim_forbidden` rejects `watermark removed` / `undetectable`.

## Grep

`rg` over `src/` `tests/official-claim-forbidden.test.ts`: no `sample` command, no `destamp`, no `undetectable` as a capability, no `watermark removed` as a verdict.

## Out of this task

- Plugin version remains `0.1.0`. Python `src/anthropies/` remains. Both later tasks.
