# Task 5 docs auditor note (raster C2PA)

Date: 2026-08-17
Role: docs auditor
Task: Raster C2PA (Family 2)
Diff: 5c498c7..59ee369 (`27238d7`, `59ee369`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT.

## Locked surfaces

- Skill YAML gained only the spec §13 line: `clean hard-bound C2PA metadata from owned png/jpg/svg (and other supported files).` Did not restore `strip a Claude watermark` or `destamp SynthID-class text`. Closing denial stays: `Does not claim official watermark removal.`
- Honesty on raster reports is `honestyStanza` (three required denials). `c2pa` honesty verb is `present` / `absent` / `removed`. After a successful strip the residual sentence is `hard-bound C2PA/metadata removed; soft-binding and pixel marks are out of scope`. Not `watermark removed`.
- Fixtures are `fixtures/c2pa/fixture-c2pa-present.png` and `.jpg`. `THIRD_PARTY.md` says they are synthetic planted `tEXt` / APP11, not signed Claude Outputs and not WR copies.
- CLI help in `src/cli.ts` is unchanged and still locked: inspect never a single watermark score; clean does not remove the keyed text mark.
- Named tests: `cert_c2pa_png_jpeg_svg`, `cert_c2patool_false_positive`, `residual_exit_not_suppressed`. Inspect/clean it-strings are planted-c2pa / drops-it. No `removesWatermark`. No `sample_watermarked.*`.
- JSDoc on new public exports is one sentence (`inspectRasterBytes`, `stripRasterBytes`, `C2pa`, `makeRasterReport`, `softBindingSentence`, `residualDrivesExit`). Capability-true.

## Grep

`rg` over new `src/formats/raster.ts` `src/services/c2pa.ts` `src/services/reporter.ts` `src/report.ts` `skills/purge-anthropies/SKILL.md` `tests/cert-c2pa.test.ts` `tests/residual-exit.test.ts` `fixtures/THIRD_PARTY.md`: no destamp, no undetectable as a capability, no official-kill claim, no `watermark removed` as a verdict, no mixed score.

## NIT

Spec §13 example test name is `removesHardBoundC2paFromPng`. Landed it-strings are `inspect sees planted c2pa and clean drops it`. Forbidden `removesWatermark` is absent; the preferred name is not used.

## Out of this task

- SVG handler is Task 6. Skill names svg because spec §13 does.
- README `undetectable` remains Task 7.
- Skill procedure still calls `python3 -m anthropies` (CLAIMS / spec §13 until Python delete).
