# Task 1 QA auditor note (Family 0 honesty patch)

Date: 2026-08-17
Role: QA
Task: Honesty patch (Family 0)
Verdict: APPROVE
Diff: `7a48c92..67597be` (`64e253b` honesty + `67597be` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| Skill/plugin/slash no longer claim C2PA or official-kill | PASS |
| `python3 -m anthropies` still present | PASS (5 invocations) |
| `docs/CLAIMS.md` exists and matches spec §13 | PASS |
| `docs/CHANNELS.md` exists and matches spec §13 / brief step 2 | PASS |
| Report step-7 grep is credible | PASS (re-ran; 0 matches) |
| No false present-tense capability on Task 1 surfaces | PASS |

## Independent grep

Re-ran the report command over `skills`, `commands`, `.claude-plugin`:

```
destamp|undetectable|official detector will fail|proves human-written|strip a Claude watermark|clean C2PA from a Claude file
```

No matches. Report exit-1 claim is credible. Skill hard-rule rewrite to `official-detector failure` (`SKILL.md:17`) is a denial, not a capability, and is why the exact banned substring is gone.

## Surfaces

- `skills/purge-anthropies/SKILL.md:3-7` YAML is the locked spec §13 block. No C2PA trigger. Denial: does not claim official watermark removal.
- Skill body keeps Python: `SKILL.md:27`, `:30`, `:42`, `:48`, `:76`.
- `commands/purge-anthropies.md:3,6` four-sentence brief copy. Denial only.
- `.claude-plugin/plugin.json:4` locked sentence; C2PA is “(once implemented)”; version `0.1.0` (`plugin.json:3`).
- `docs/CLAIMS.md` has forbidden phrases, locked honesty stanza, channel verbs, three denials, locked skill YAML, locked plugin description, CLI help sense, Report field meanings, demo last-stanza rules.
- `docs/CHANNELS.md` has `deterministic | c2pa | official | statistical`, certificate vs best-effort, demo four rows + honesty twice, never a success banner.

## Findings

BLOCKER: none

MAJOR: none on Task 1 file list.

NIT: `.claude-plugin/plugin.json:12` keywords still include `watermark` / `humanize`. Brief said description only; leave.

NIT: `skills/purge-anthropies/SKILL.md:54` “Target well under 50% surviving 5-grams” is operator rewrite guidance, not an official-kill claim. Residual already disclosed at `:62`.

Residual (out of file list, not a Task 1 fail): `README.md:150` non-quotation `undetectable`. Spec §13 forbids it. Task 7 / README honesty box owns it.

## Not done (correct)

No TypeScript. Python not deleted. Plugin version not bumped. C2PA not restored to skill triggers.
