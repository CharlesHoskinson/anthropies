# Task 5 QA auditor note (raster C2PA)

Date: 2026-08-17
Role: QA
Task: Raster C2PA (Family 2)
Verdict: APPROVE
Diff: `5c498c7..59ee369` (`27238d7` feat + `59ee369` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| TDD evidence (RED then GREEN) | PASS |
| `cert_c2pa_png_jpeg_svg` PNG/JPEG rows | PASS |
| `residual_exit_not_suppressed` | PASS |
| `cert_c2patool_false_positive` | PASS |
| Soft-binding residual on image reports | PASS |
| No official-detect lie | PASS |

## TDD

RED documented as missing `../src/services/c2pa.js`, `residualDrivesExit is not a function`, and inspect planted C2PA `expected 2 to be 1` (BinaryInput before raster path). Stronger than Tasks 2–4 (assertion fail, not only missing module). GREEN: 21 tests / 9 files. Feat commit `27238d7` matches brief Step 8.

## Surfaces

- `tests/cert-c2pa.test.ts` `cert_c2pa_png_jpeg_svg`: PNG and JPEG rows. Inspect planted → `c2pa` present, `degraded` false. Clean drops it. Cleaned PNG starts `\x89PNG`; JPEG `\xff\xd8\xff`. After-inspect no `c2pa` present. SVG is Task 6 (correct).
- Soft-binding: PNG inspect-before and both after-inspects match `/soft-binding/`. `makeRasterReport` (`src/services/reporter.ts:80-112`) always concatenates `softBindingSentence`. Successful strip honesty is `hard-bound C2PA/metadata removed; soft-binding and pixel marks are out of scope`.
- `cert_c2patool_false_positive`: mock `No claim found` / `No JUMBF data found` ⇒ `hasManifestFromToolOutput === false`. Live `c2patool` skip-if-absent. Missing tool is not a Fail.
- `residual_exit_not_suppressed`: leftover Report with `c2pa` present drives `residualDrivesExit`; `degraded: true` does not. Inspect planted fixture exits 1 in human and `--json`. Successful strip exits 0 both modes. CLI inspect/clean map residual via `ResidualHits` → teardown 1 (`src/cli.ts:62-64,86-88,173-175`).
- Official: raster findings `official: unavailable`, `report.official` is `OfficialUnavailable`, honesty `unavailable (ANTHROPIC_DETECT_URL unset)` plus `this run does not prove the official Claude text detector will fail`. No score field. Fixtures are synthetic, not a Claude signature. `THIRD_PARTY.md` says they do not prove an official detector result.

## Findings

BLOCKER: none

MAJOR: none on Task 5 named tests.

NIT: leftover-after-clean exit 1 is a `residualDrivesExit` unit test, not a CLI clean that actually leaves a planted chunk. Inspect residual and happy-strip are subprocess-asserted. Same class as Task 4 teardown wiring.

NIT: JPEG inspect-before does not assert `/soft-binding/` (PNG does). Product always appends it.

NIT: WebP/AVIF/HEIC/GIF classify as `raster` and inspect reports `c2pa: absent` (not `unavailable`/`degraded`). Disclosed; this task certifies PNG/JPEG only.

## Not done (correct)

SVG C2PA (Task 6). Live `c2patool` on default `R`. WR fixture bytes / `NOTICE` change. Python `src/anthropies/` remains.
