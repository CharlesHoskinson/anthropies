# Task 7 STYLE auditor note (capture, demo, delete Python)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Capture, demo, delete Python (Family 6)
Diff: `cdec59b..4863f80` (`1cc6a52` feat + `4863f80` wiki ingest)
Scope: Capturer Effect.Service, HttpClient only on capture/demo Live, Config not process.env, Fail PreMarkModel/MissingApiKey, no node:fs, @effect/cli, ESM, 0.2.0

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 7 / spec §5 / §9 / §11)

Style fails on forbidden imports, shared AppR, Fail/Finding mix, non-`@effect/cli` argv, Capturer not `Effect.Service`, `HttpClient` on inspect/clean, `process.env`, `node:fs`, version ≠ 0.2.0, or missing ESM/`@effect/cli`.

| Check | Result | Cite |
|---|---|---|
| Capturer Effect.Service + Default | PASS | `src/services/capturer.ts:201-206` `Effect.Service` + `accessors: true` + `succeed`. Capture `R` is `FileSystem \| HttpClient`. Wired in CLI services (`cli.ts:152-158`). No typed AppR. |
| HttpClient only on capture/demo Live | PASS | `NodeHttpClient.layer` only on capture/demo handlers (`cli.ts:136,139`). Inspect/clean/humanize stay FileSystem. `yield* HttpClient` is after allowlist + key (`capturer.ts:84-98`). Default `pnpm test` premark path never constructs it. Live test provides the layer only after `Config` `LIVE=1` (`tests/live-capture.test.ts:13-28`). Deterministic core has no HttpClient import. |
| Config, not process.env | PASS | `anthropicApiKey = Config.option(Config.redacted("ANTHROPIC_API_KEY"))` (`config.ts:9`). Detect URL and rewrite knobs are Config. Live skip uses `Config.string("LIVE")` (`live-capture.test.ts:13`). No `process.env` in src/tests. `process.argv` only at `CliCommand.run` (`cli.ts:195`). |
| Fail PreMarkModel / MissingApiKey | PASS | TaggedErrors (`fail.ts:16-25`). Unknown model → `PreMarkModel` before HTTP or key (`capturer.ts:84-89`). Unset key → `MissingApiKey` (`capturer.ts:91-96`). CLI failTags → exit 2 (`cli.ts:42-49,186-188`). Official unavailable is a Finding (`official-unavailable.test.ts`). |
| no node:fs | PASS | Load/write via platform `FileSystem` (`capturer.ts:97,120-130`). Tests: `FileSystem` + `NodeContext`. No `node:fs` / `fs/promises` / `node:child_process` / `fetch`. |
| @effect/cli | PASS | `CliCommand.make` + Options/Args + `withSubcommands` + `run` + `NodeRuntime.runMain` (`cli.ts:1-3,132-150,194`). No commander/yargs. |
| ESM / 0.2.0 | PASS | `"type": "module"`, NodeNext, verbatimModuleSyntax (`package.json:5`, `tsconfig.json:7-10`). Version `0.2.0` in package, plugin, `CliCommand.run` (`package.json:3`, `.claude-plugin/plugin.json:3`, `cli.ts:150`). No `any`, `as unknown as`, `console.log`, `Effect.runPromise` in library TS. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **spec §5.1 Test layers** — `Capturer` exposes `Default` only (`succeed` with Live HTTP). Tests use `Capturer.Default` (`tests/premark-model.test.ts:9`) rather than a first-class `Test`. Same residual as Tasks 3–6. Premark fails before `yield* HttpClient`, so the gate does not depend on a Test double.

2. **Dual allowlist + orDie** — `allowlist.ts` and `allowlist.json` are both `[]` (`allowlist.ts:2`). HTTP/parse/FS after an allowlisted model are `orDie` (`capturer.ts:109-130`). Demo catches only Fail (`capturer.ts:174-177`), so timeout/rate-limit die instead of skip (spec §11.5 is demo script policy; Fail set stays `PreMarkModel` / `MissingApiKey`).

## Not defects

- `NodeHttpClient.layer` on `demo` even when capture is skipped is spec §5.2 (`capture` / `demo` Live `R`).
- `bodyUnsafeJson` is the request body; sidecar is Schema-encoded. Not a Fail/Finding mix.
- Official unavailable remains a Finding. Capture Fails do not leak into inspect.
- Python delete is docs/package, not a TS style defect.
- `node:url` in the allowlist json test is not a forbidden import.

## Conclusion

Task 7 matches spec §5 / §9 / §11 and the STYLE hard gate: `Capturer` is `Effect.Service`, HttpClient only on capture/demo Live, Config not `process.env`, Fails are `PreMarkModel`/`MissingApiKey`, no `node:fs`, `@effect/cli`, ESM, `0.2.0`. APPROVE.
