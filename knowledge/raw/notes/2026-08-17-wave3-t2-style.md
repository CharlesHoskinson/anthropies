# Wave 3 Task 2 STYLE auditor note (Family 2 stronger prompts)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Stronger Layer B prompts (clause-order / H-gram break; keep facts/URLs/fences)
Diff: `d9a9873..HEAD`
Scope: prompts are constants, structure-rule tests exist, no `node:fs`

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 0

## STYLE gate (Wave 3 spec §3 Family 2)

Style fails if prompts are built at runtime from disk or interpolated templates, if print-prompt has no structure-rule tests, or if the family adds `node:fs`.

| Check | Result | Cite |
|---|---|---|
| Prompts are constants | PASS | `src/services/humanizer.ts:49-78` `export const PROSE_PROMPT` / `CODE_PROMPT` are static backtick strings (no `${}`). `promptFor` (`:116-117`) is a ternary select. Concatenation is `promptFor(...) + cleaned` only (`:186`, `:197`). |
| Tests exist | PASS | `tests/humanize-print-prompt.test.ts` `humanize_prompt_structure_rules`: prose + code print-prompt must `startsWith` the constants, match clause-order / sentence-boundary / discourse-marker / H-gram / facts/URLs/fences, and must not match `undetectable` / `beats detector`. |
| no `node:fs` | PASS | Grep of `src/` + `tests/`: no `node:fs`, `from "fs"`, or `require("fs")`. Tests use `@effect/platform-node` `NodeContext.layer`. Writes stay `Reporter` + platform `FileSystem`. |
| print-prompt default | PASS | Family 2 does not add HTTP or destamp. Structure tests still expect `metric.status === "not-run"`. |
| Forbidden imports | PASS | Changed files have no `node:http`, `fetch`, `process.env`, `Effect.runPromise`, `as unknown as`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

None.

## Not defects

- `NodeContext.layer` on structure-rule tests is the existing `Humanizer.humanize` `R = FileSystem` signature, not a `node:fs` import.
- Regex contract tests rather than `toMatchSnapshot` — constants stay the source of truth.
- Family 3 README / skill title-restoration copy is later.

## Conclusion

Family 2 matches the STYLE hard gate: prompts are constants, print-prompt structure tests exist, no `node:fs`. APPROVE.
