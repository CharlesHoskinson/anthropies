# Task 3 QA auditor note (classify + Layer A + text inspect/clean)

Date: 2026-08-17
Role: QA
Task: Classify + Layer A + text inspect/clean (Family 1)
Verdict: APPROVE
Diff: `68995de..1975bbf` (`fa96737` feat + `1975bbf` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| TDD evidence (RED then GREEN) | PASS |
| `cert_layer_a_roundtrip` exists and matches brief | PASS |
| Inspect trailer → exit 1 | PASS |
| PNG → `BinaryInput` exit 2, no write | PASS |
| `--json` stdout parseable | PASS |
| `binary_guard` / `write_guard` | PASS |

## TDD

RED documented as missing `../src/layer-a.js` (cert), then missing `../src/services/cleaner.js` (binary/write). Brief Step 2 expected `applyLayerA` not defined. Observed (module missing). GREEN: 10 tests / 5 files after implementation. Single feat commit `fa96737` matches brief Step 9. Weak RED (missing module, not a failing assertion) is brief-compliant, same as Task 2.

## Surfaces

- `tests/cert-layer-a.test.ts` is the brief `cert_layer_a_roundtrip` verbatim. Claude trailer stripped, human trailer kept, Generated-with banner stripped, ZWSP stripped, emoji ZWJ kept.
- Inspect residual: `Detector.deterministic` → `present` when Layer A would remove; CLI `ResidualHits` → teardown exit 1 (`src/cli.ts:58-60,139-141`). `json_stdout_purity` asserts `JSON.parse(stdout)` + `Schema.decodeUnknownSync(Report)` + exit 1 on `fixtures/layer-a/trailer-claude.txt`.
- PNG magic `\x89PNG` classifies `raster` (`src/kind.ts:66-68`). No text handler → `BinaryInput` (`inspector.ts:28-32`, `cleaner.ts:39-43`) before any write. Teardown maps Fail tags to exit 2 (`cli.ts:143-145`). `binary_guard` / `write_guard` assert no dest, no `.bak`, in-place bytes unchanged.
- `Reporter.print`: honesty on `Console.error`; `--json` is one `Schema.encode` line on `Console.log` (`reporter.ts:85-89`). `disableErrorReporting: true`.
- `write_guard`: failed classify + `--in-place` does not write; symlink dest is `WriteGuard`.

## Findings

BLOCKER: none

MAJOR: none on Task 3 file list.

NIT: `describe("binary_guard_docx_png_stdin")` names stdin; no stdin/JPEG/PDF CLI case. Brief Step 4 is PNG + DOCX `PK` + `--force-text` only. Spec §16.2 stdin/JPEG/PDF wait for later families.

NIT: spec §16.2 `cert_layer_a_roundtrip` also wants bidi + second inspect zero hits. Brief Step 1 is the verbatim unit test; that is the gate.

NIT: `json_stdout_purity` compiles `tsc` then invokes inspect twice (`Command.string` + `Command.exitCode`). PNG CLI exit 2 is wired, not subprocess-tested.

## Not done (correct)

Raster / markup / office / PDF handlers. `humanize` / `capture` / `demo` stubs. Official stays `Unavailable`. No `HttpClient`. Python `src/anthropies/` remains.
