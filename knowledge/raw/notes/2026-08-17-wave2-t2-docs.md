# Wave 2 Task 2 docs auditor note (OpenAPI + serve CLI)

Date: 2026-08-17
Role: docs auditor
Task: OpenAPI document and `anthropies serve` (Wave 2 family 2)
Verdict: APPROVE
Diff: `1dab5ee..4213733` (`673e338` feat + `4213733` wiki ingest)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 2 NIT (README/CLAIMS out of this task’s file list).

## Required checks

| Check | Result |
|---|---|
| Honesty | PASS — OpenAPI info: “Honesty remains on report.honesty.” Report schema: “honesty is the locked stanza. No score field.” Serve description does not humanize and never claims official text-kill. |
| No official-kill | PASS — OpenAPI: “Does not claim official-detector failure.” Clean summary: “Does not remove the keyed text mark.” `officialDetect` is “Not a score.” `serve-cli` rejects `watermark removed` / `undetectable`. |
| Version `0.3.0` | PASS — `package.json`, `serviceVersion`, `CliCommand.run(..., { version: "0.3.0" })`, OpenAPI `info.version` + `/health` enum. |
| `serve --help` | PASS — host/port descriptions name `127.0.0.1` and `8765`; remote bind needs explicit `--host`. Test `anthropies serve --help defaults to loopback 127.0.0.1:8765`. |
| Product sentence | PASS — package + root CLI description still the locked one-liner. |

## Locked surfaces (this task)

- `src/http/openapi.ts`: OpenAPI `3.0.3`. Paths GET `/health` `/capabilities` `/openapi.json`, POST `/inspect` `/clean`. No `/humanize`. Servers `http://127.0.0.1:8765`.
- `src/cli.ts`: `serve` description “Serve inspect and clean on loopback. Does not humanize. Never claims official text-kill.” Version `0.3.0`.
- `tests/serve-cli.test.ts`: help names loopback; forbids destamp copy.

## Grep

`rg` over `src/http/openapi.ts`, `src/cli.ts`, `tests/serve-cli.test.ts`, `tests/http-server.test.ts`: no `destamp`, no official-kill as a capability, no `watermark removed` as a verdict. Denial substrings appear only as required CLAIMS lines.

## NIT

- README “Version is `0.2.0`” and command table omit `serve`. Task 1 already reserved this for Task 2/3. File list is OpenAPI + `cli.ts` serve. Task 3 skill/README.
- `docs/CLAIMS.md` still “Wave 1 public-copy contract”; no `serve` row. Same residual.

## Not this task

Skill HTTP path, Docker, CI. `/openapi.json` behind bearer is spec-consistent, not a copy defect.
