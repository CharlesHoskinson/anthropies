# Wave 2 Task 3 QA auditor note (Skill HTTP path)

Date: 2026-08-17
Role: QA
Task: Skill HTTP client path (Wave 2 family 3 / Task 3)
Verdict: APPROVE
Diff: `0524caa..9c05bb9` (`216e961` docs + `9c05bb9` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| Health-first | PASS |
| Base64 curl `{ file, name }` | PASS |
| Fallback `npx anthropies` | PASS |
| No official-kill | PASS |
| No HTTP `/humanize` | PASS |

## Surfaces

- `skills/purge-anthropies/SKILL.md` step 2 prefers `ANTHROPIES_SERVICE_URL` (default `http://127.0.0.1:8765`). `anthropies serve` is named as the FileSystem owner (`npx anthropies serve` / `node dist/cli.js serve`).
- Health-check first: `curl -sS -f "$ANTHROPIES_SERVICE_URL/health"` after `: "${ANTHROPIES_SERVICE_URL:=http://127.0.0.1:8765}"`. Expected `{"ok":true,"version":"0.3.0"}`. Fail path: say so; do not invent a successful HTTP result; stop if the operator required the service.
- Local fallback is `npx anthropies clean --in-place <path>` (repo: `node dist/cli.js`). Matches plan Task 3. Design §2 "do not invent a local fallback that contradicts the operator" is gated: fallback only when the operator did not require the service.
- When health succeeds: `file_b64=$(base64 < <path> | tr -d '\n')` + `name=$(basename <path>)`; `POST /inspect` and `POST /clean` with `{"file","name"}`. `cleaned` is documented as base64. Bearer header only when `ANTHROPIES_SERVER_API_KEY` is set.
- Humanize stays CLI-only (`npx anthropies humanize`). "There is no HTTP `/humanize`." Official stays `unavailable` unless `ANTHROPIC_DETECT_URL` is set. Hard rule + Do-not row: do not claim official-detector failure. Residual report: not an official-detector certificate.
- README version `0.3.0`; `npx anthropies serve`; loopback `127.0.0.1:8765`; skill health-checks first; no HTTP `/humanize`. Honesty box unchanged. No destamp. Capture not retargeted as sample.

## Findings

BLOCKER: none

MAJOR: none

NIT: CLI fallback example is `clean --in-place` only; inspect HTTP has no matching `npx anthropies inspect` fallback line.

NIT: Bearer is prose ("add `-H ...`") not shown on the inspect/clean curl lines.

NIT: Examples use POSIX `base64` / `basename` (implementer disclosed; Windows needs WSL).

## Not done (correct)

Docker / compose (Task 4). CI (Task 5). `commands/purge-anthropies.md` and `docs/CLAIMS.md` serve row left untouched (out of file list).
