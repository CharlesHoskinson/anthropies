# Wave 2 Task 5 QA auditor note (GitHub Actions CI)

Date: 2026-08-17
Role: QA
Task: GitHub Actions CI for pnpm test and pnpm build (Wave 2 family 6 / plan Task 5)
Verdict: APPROVE
Diff: `1c02563..8c5c396` (`8c5c396` feat + implementer wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| Workflow present | PASS |
| Triggers: push + pull_request → main | PASS |
| Matrix OS: ubuntu-latest AND windows-latest | PASS |
| Commands: `pnpm test` AND `pnpm build` | PASS |
| Node 22 (matches engines.node >=22) | PASS |
| Install uses lockfile (`pnpm install --frozen-lockfile`) | PASS |
| No heavy backends | PASS |
| No live tests / no ANTHROPIC_API_KEY required | PASS |
| No /humanize | PASS |
| Honesty: no official-kill claim | PASS |

## Surfaces

- Only workflow: `.github/workflows/ci.yml` (35 lines). Valid YAML. Job `ci` with `runs-on: ${{ matrix.os }}`.
- Triggers: `on.push.branches: [main]` and `on.pull_request.branches: [main]`. No other events.
- Matrix: `os: [ubuntu-latest, windows-latest]`. Both required OSes present.
- Steps: `actions/checkout@v4` → `pnpm/action-setup@v4` (`version: 11.22.0`, same pin as Docker) → `actions/setup-node@v4` (`node-version: 22`, `cache: pnpm`) → `pnpm install --frozen-lockfile` → `pnpm test` → `pnpm build`.
- `package.json` scripts exist: `test` = `vitest run`, `build` = `tsc -p tsconfig.json`. `engines.node` is `>=22`. `pnpm-lock.yaml` is on the tree (`lockfileVersion: '9.0'`), so frozen-lockfile is not a blocker.
- `pnpm test:live` is a separate script (`LIVE=1 vitest run tests/live-capture.test.ts`). Workflow does not invoke it. Default `vitest run` includes `tests/live-capture.test.ts`, but `live_capture_smoke` returns before HTTP unless `LIVE=1` and `ANTHROPIC_API_KEY` and a pinned model ID. Missing key is a skip, not a required-CI fail.
- No apt / choco / winget. No qpdf / exiftool / c2patool. No extra job that can flake the required surface.
- Family does not add HTTP `/humanize`. Workflow text has no destamp / official-kill / undetectable claim. Official stays unavailable unless `ANTHROPIC_DETECT_URL` is set (unset on runners). Version stays `0.3.0`.

## Local evidence

Did not run `pnpm test` / `pnpm build` in this auditor context (optional). Verdict is static inspection of the workflow family plus `package.json` / lockfile / live-skip.

## Findings

BLOCKER: none

MAJOR: none

NIT: unpinned action SHAs — `actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4` are floating major tags.

NIT: no `workflow_dispatch`. Not required by §2.

NIT: Windows matrix is unproven until the first Actions run (implementer disclosed). Tests look path-safe; a Windows-only fail will show on GitHub.

## Not done (correct)

Installing qpdf/exiftool/c2patool on runners. `pnpm test:live`. Requiring `ANTHROPIC_API_KEY`. HTTP `/humanize`. Official-kill / destamp claims. Extra lint or live-capture jobs.

Later commit `f4e9828` ignored. HTTP / Docker / skill families not re-litigated.
