# Wave 2 Task 2 QA auditor note (OpenAPI + serve CLI)

Date: 2026-08-17
Role: QA
Task: OpenAPI document and `anthropies serve` (Wave 2 family 2)
Verdict: APPROVE
Diff: `1dab5ee..4213733` (`673e338` feat + `4213733` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| `GET /openapi.json` is OpenAPI `3.0.3` | PASS |
| Paths `/health` `/capabilities` `/inspect` `/clean` | PASS |
| No `/humanize` in document or router | PASS |
| `anthropies serve` defaults `127.0.0.1:8765` | PASS |
| Named tests exist | PASS |

## Surfaces

- `src/http/openapi.ts` exports `openApiDocument` with `openapi: "3.0.3"`, `info.version` from `serviceVersion` (`0.3.0`), servers `http://127.0.0.1:8765`. Paths: GET `/health`, GET `/capabilities`, GET `/openapi.json`, POST `/inspect`, POST `/clean`. No `/humanize`. Info/clean copy deny humanize and official-detector failure. No score field.
- `src/http/server.ts` serves `GET /openapi.json` as `HttpServerResponse.unsafeJson(openApiDocument)` on the same `authorize` router. HTTP layer does not import `node:http`.
- `src/cli.ts` `serve --host --port` uses `Options.withDefault(defaultServeHost)` / `defaultServePort`. Remote bind needs explicit `--host`. CLI still has `humanize` (CLI-only). `CliCommand.run` version `0.3.0`.
- `tests/http-server.test.ts` `GET /openapi.json is OpenAPI 3.0.3 with inspect and clean paths` boots `HttpApp` via `NodeHttpServer.layerTest`, asserts `3.0.3`, the four locked paths, and `not.toHaveProperty("/humanize")`.
- `tests/serve-cli.test.ts` builds CLI and checks `serve --help` names `--host` `127.0.0.1` `--port` `8765`; rejects `watermark removed` / `undetectable`.

## Findings

BLOCKER: none

MAJOR: none

NIT: Loopback default is source + help-description, not a live listen. Effect CLI does not print `withDefault` values; the test would still pass if `withDefault` drifted while the description stayed.

NIT: Named OpenAPI test does not assert `/openapi.json` inside `paths` or that `/inspect`/`/clean` are POST.

NIT: No HTTP assertion that `POST /humanize` is absent. Router `catchAll` would 400, not run Layer B.

## Not done (correct)

Skill HTTP path, Docker (`--host 0.0.0.0`), CI. Live bind / `0.0.0.0` is Task 4.
