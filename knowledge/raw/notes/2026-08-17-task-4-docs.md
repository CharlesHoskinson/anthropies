# Task 4 docs auditor note (humanize print-prompt + origin blocklist)

Date: 2026-08-17
Role: docs auditor
Task: Humanize print-prompt + origin blocklist (Family 1)
Diff: 55e669b..6c3f73f (`7781e0a`, `6c3f73f`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT.

## Locked surfaces

- CLI help in `src/cli.ts` keeps spec §13 / CLAIMS.md sense. `inspect` never a single watermark score. `clean` does not remove the keyed text mark. `humanize` is “Rewrite wording on a non-origin model (best-effort). Refuses Claude and Gemini.” `demo` never claims official text-kill. Root is still the product sentence.
- Print-prompt does not claim a rewrite or official-detector kill. Output is the ported `PROSE_PROMPT` / `CODE_PROMPT` plus Layer-A-cleaned text. Stderr note: `print-prompt: run this on an unmarked local model`. `rewrite_metric.status = "not-run"`. `surviving_ratio` is null.
- `reportFromHumanize` keeps `makeTextReport` honesty (three required denials; statistical line is best-effort / not-run; “not an official-detector certificate”). `Reporter.print` writes the stanza to stderr. JSON honesty is the same array. Test `humanize_print_prompt_default` matches `/best-effort/` and rejects `/watermark removed/i`.
- `OriginBlocked` reason is `origin model would re-stamp the mark`. Refuse copy, not a kill claim. CLI maps it to exit 2 (already in `failTags`).
- JSDoc on new exports is one sentence. `unicodeWords` is “Not a watermark tokenizer.” `notRunMetric` is a skipped rewrite. `RewriteMetric` in `report.ts` stays “Not a watermark score.”
- Named tests: `origin_blocklist`, `humanize_print_prompt_default`. No `removesWatermark`. No new `sample_watermarked.*` fixtures.

## Grep

`rg` over new `src/services/humanizer.ts` `src/rewrite-metric.ts` `src/cli.ts` `tests/origin-blocklist.test.ts` `tests/humanize-print-prompt.test.ts`: no destamp, no undetectable as a capability, no official-kill claim, no `watermark removed` as a verdict.

`PROSE_PROMPT` “almost no original 5-word sequence survives” is operator rewrite guidance to a non-origin model, not a user-facing removal verdict.

## NIT

`Humanizer` class JSDoc: “Origin blocklist plus print-prompt rewrite.” Spec §11.3 and the brief say print-prompt returns the prompt, not a rewrite. Not a CLAIMS lie.

## Out of this task

- README `undetectable` at README.md:150 remains Task 7.
- ollama / openai-compatible HTTP not implemented; fallback note is honest (`{backend} not implemented; falling back to print-prompt`).
