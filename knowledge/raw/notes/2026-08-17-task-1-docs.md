# Task 1 docs auditor note (Family 0 honesty patch)

Date: 2026-08-17
Role: docs auditor (hard gate)
Task: Honesty patch (Family 0)
Diff: 7a48c92..67597be (`64e253b`, `67597be`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 3 NIT (out of this task’s file list).

## Locked surfaces

- Skill YAML in `skills/purge-anthropies/SKILL.md:3-7` matches spec §13 verbatim. `python3 -m anthropies` remains (clean ×2, humanize print-prompt, humanize ollama, `--help`). C2PA trigger not restored.
- Plugin `description` in `.claude-plugin/plugin.json:4` is the locked sentence, including the spec’s curly apostrophe in “Anthropic’s”. `version` stays `0.1.0`.
- Slash `commands/purge-anthropies.md:3` and body line 6 are the brief’s four sentences.
- `docs/CLAIMS.md` has forbidden phrases, locked honesty stanza (lines 31–40 = spec §8), channel verbs, three denials, locked skill YAML, locked plugin description, CLI help sense, Report field meanings, demo last-stanza rules.
- `docs/CHANNELS.md` has `deterministic | c2pa | official | statistical`, certificate vs best-effort, demo four-row table + honesty twice, no success banner.
- `docs/MANIFESTO.md` Sources no longer cites missing `knowledge/raw/legal-opus{1,2,3}-brief.md`. Those files are not in the worktree.

## Grep

`rg -n -i "destamp|undetectable|official detector will fail|proves human-written|strip a Claude watermark|clean C2PA from a Claude file" skills commands .claude-plugin` — no matches.

Skill hard-rule rewritten to `Do not claim official-detector failure` so the substring does not fire. Honesty stanza denial is `does not prove the official Claude text detector will fail` (required text, not a capability claim).

## NITs (not this task)

- `README.md:150` — non-quotation `undetectable`. Task 7 README honesty box.
- `docs/MANIFESTO.md:33` — paraphrase uses `undetectable` outside the quotation on line 35. Pre-existing; task only deleted the raw citation.
- `.claude-plugin/plugin.json:12` — keywords still include `watermark` / `humanize`. Brief said description only.
