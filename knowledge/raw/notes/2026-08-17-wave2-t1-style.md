# Wave 2 Task 1 STYLE auditor note (HTTP inspect/clean)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: HTTP inspect/clean service (Wave 2 family 1)
Diff: `ad0f955..4f3819c` (`43acd73` feat + `4f3819c` wiki ingest)
Scope: `@effect/platform` HttpServer not `node:http`, FileSystem temps, Fail≠Finding, no `node:fs`

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 3

## STYLE gate (plan: Task 1 / Wave 2 spec §2–§3)

Style fails on `node:http`, `node:fs`, Fail/Finding mix, `process.env`, or a non-platform HTTP stack.

| Check | Result | Cite |
|---|---|---|
| `@effect/platform` HttpServer | PASS | `src/http/server.ts:1-6,184` `HttpRouter` + `HttpServer.serve`. No express/fastify/hono/`node:http`. Tests boot via `NodeHttpServer.layerTest` (`tests/http-server.test.ts:2,11-14`). |
| FileSystem temps | PASS | `writeUpload` uses `FileSystem.makeTempDirectoryScoped` then `writeFile` (`server.ts:64-73`). No `inspectBytes`. Scoped dir + `safeUploadName` (`server.ts:54-62`). |
| no `node:fs` | PASS | Library/tests import `@effect/platform` `FileSystem` only. Fixture path via `node:url` (`http-server.test.ts:5,9`) — not forbidden. |
| Fail ≠ Finding | PASS | Fails stay `Schema.TaggedError` (`fail.ts`). HTTP maps `BinaryInput`/`DecodeError`/`InputTooLarge`/`WriteGuard`/`OriginBlocked` → 400 (`server.ts:79,95-104`). Findings stay on 200 `Report` (`InspectResponse`/`CleanResponse`). Official `unavailable` is a Finding. Missing tools stay `degraded` Findings, not 400. |
| Config not `process.env` | PASS | `serverApiKey = Config.option(Config.redacted("ANTHROPIES_SERVER_API_KEY"))` (`config.ts:15`). Tests use `ConfigProvider.fromMap`. |
| Forbidden imports | PASS | No `node:fs` / `node:http` / `fetch` / `process.env` / `console.log` / `Effect.runPromise` / `any` / `as unknown as` / default exports in new TS. Effect 3.22.1. Package `0.3.0`. |
| Honesty / no `/humanize` | PASS | Routes are health/capabilities/inspect/clean only (`server.ts:172-176`). Report is Wave 1 Schema pass-through. Test asserts no `"score"` and official `Unavailable`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **`src/http/server.ts:69-70`** — temp `writeFile` PlatformError is remapped to `DecodeError`. Wave 1 §9.1 `DecodeError` is truncated image / zip bomb / over budget / undecodable container. A scoped-temp write fail is not a decode. Still a Fail (not a Finding). HTTP 400 is right; the tag is a lie.

2. **`src/http/server.ts:81-104`** — `catchHttpFail` duck-types `_tag` via `tagOf` + a string `Set`, not `Effect.catchTags`. Fail set is stringly. Unknown errors become 400 `bad_request`. Does not mix Finding onto the error path.

3. **`src/http/schema.ts:50-53`** — `HealthResponse` is unused on the encode path. `health` is `unsafeJson({ ok: true, version })` (`server.ts:116`). Success inspect/clean do `Schema.encodeUnknownSync`. Harmless.

## Not defects

- `NodeHttpServer.layerTest` lives in tests. Library `HttpApp` is `HttpServer.serve(router)` and does not import `node:http`. Task 2 binds loopback the same way.
- Test `HttpClient` is the in-process client from `layerTest`, not capture Live outbound. Inspect/clean `R` still has no `HttpClient`.
- `capabilities.scorers.officialDetect` is a presence bit (URL set), not a score and not a Finding. Adapter still `OfficialUnavailable` on inspect/clean. Honesty residual, not a Fail/Finding mix.
- Bearer string-compare on loopback. Not a STYLE gate.
- CLI help still `0.2.0`. Task 2 `serve`.
- No `/openapi.json`, no `/humanize`. Correct for this family.

## Conclusion

Family 1 matches Wave 2 spec §2–§3 and the STYLE hard gate: platform HttpServer, FileSystem scoped temps, Fail≠Finding, no `node:fs`. APPROVE. Do not hold Task 2 for the NITs.
