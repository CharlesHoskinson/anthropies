# Wave 2 Task 1 docs auditor note (HTTP inspect/clean)

Date: 2026-08-17
Role: docs auditor
Task: HTTP inspect/clean service (Wave 2 family 1)
Verdict: APPROVE
Diff: `ad0f955..4f3819c` (`43acd73` feat + `4f3819c` wiki ingest)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 3 NIT (2 out of this task’s file list).

## Required checks

| Check | Result |
|---|---|
| Honesty on `report.honesty` | PASS — Wave 1 `honestyStanza` pass-through; inspect test locks `does not prove the official Claude text detector will fail`; no paraphrase as “watermark gone” |
| No official-kill | PASS — no destamp / undetectable-as-claim / `official detector will fail` as a capability / `strip a Claude watermark` / mixed score |
| Version `0.3.0` | PASS — `package.json`, `serviceVersion`, `GET /health`, `GET /capabilities` |
| CLAIMS.md + Wave 2 spec | PASS — Report schema unchanged; four unmixed channels; `/clean` is not Layer B; official stays `unavailable` |
| Product sentence | PASS — `package.json` description still the locked one-liner |

## Locked surfaces (this task)

- `src/http/schema.ts`: `serviceVersion = "0.3.0"`. JSDoc: officialDetect is “a presence bit, not a score”; inspect “Honesty stays on report.honesty.”
- `src/http/server.ts`: routes health / capabilities / inspect / clean only. Comment: “No /humanize.” `HttpApp` wraps Wave 1 Inspector + Cleaner.
- `tests/http-server.test.ts`: trailer fixture → deterministic `present`, official `unavailable`, no `"score"`. Clean strips the Claude trailer, not a watermark verdict.
- Wiki ingest is honest: official stays unavailable; no `/humanize`; residuals named (serve, OpenAPI, skill, Docker, CI).

## Grep

`rg` over `src/http/`, `tests/http-server.test.ts`, new wiki notes: no `destamp`, no `undetectable`, no official-kill, no `watermark removed` as a verdict. The denial substring appears only as the required CLAIMS line.

## NIT

- CLI `CliCommand.run(..., { version: "0.2.0" })` and README “Version is `0.2.0`” while `package.json` is `0.3.0`. Disclosed. Task 2 (`serve`) / Task 3 (skill). Out of Task 1 file list.
- `capabilities.scorers.officialDetect` is URL-set, not adapter-live. JSDoc is honest; wiki synthesis omitted the residual the raw implementer note named.
- Implementer synthesis “Locked surfaces now match §2 / §3” is slightly broad (§2 also lists OpenAPI, bind, skill). Immediate bullet list is Task 1-only and correct.

## Not this task

OpenAPI, `anthropies serve`, skill HTTP copy, Docker, CI. `docs/CLAIMS.md` still says “Wave 1 public-copy contract” and does not mention HTTP — not in the brief file list.
