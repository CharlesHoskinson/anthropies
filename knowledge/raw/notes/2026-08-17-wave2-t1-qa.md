# Wave 2 Task 1 QA auditor note (HTTP inspect/clean)

Date: 2026-08-17
Role: QA
Task: HTTP inspect/clean service (Wave 2 family 1)
Verdict: APPROVE
Diff: `ad0f955..4f3819c` (`43acd73` feat + `4f3819c` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| `tests/http-server.test.ts` boots `HttpApp` in-process | PASS |
| Trailer fixture as base64 → deterministic `present` | PASS |
| Official `unavailable` on inspect and clean | PASS |
| Bearer missing/wrong → 401 when key set | PASS |
| 256 MiB cap (`fileCapBytes` + `decodeRequestFile`) | PASS |
| No HTTP `/humanize` | PASS |
| Honesty stanza + no `"score"` | PASS |

## Surfaces

- Named test: `inspects trailer fixture with deterministic present and official unavailable` POSTs `fixtures/layer-a/trailer-claude.txt` (`Fix the bug` + `Co-Authored-By: Claude <noreply@anthropic.com>`) as base64 via `NodeHttpServer.layerTest`.
- Inspect: `kind=text`, deterministic `present`, official `unavailable`, `report.official._tag === "Unavailable"`, honesty matches `does not prove the official Claude text detector will fail`, `JSON.stringify(report)` has no `"score"`.
- Clean: 200 + `cleaned` base64 without `noreply@anthropic.com`, still `Fix the bug`; official stays `unavailable`. Cleaner is Layer A / format clean only — no `Humanizer`.
- Bearer: `KeyedLive` with `ANTHROPIES_SERVER_API_KEY=secret`. Missing and `bearerToken("nope")` → 401 `{ ok: false, error: "unauthorized" }`. Valid bearer → 200 health `0.3.0`.
- Cap: `fileCapBytes = 256 * 1024 * 1024`. `decodeRequestFile` rejects over-cap (`InputTooLarge`) and invalid base64 (`DecodeError`). Server maps both to 400. Helper test uses `decodeRequestFile("AAAA", 1)`.
- Router: `GET /health`, `GET /capabilities`, `POST /inspect`, `POST /clean` only. Comment and tree: no `/humanize`. CLI `humanize` unchanged (correct; CLI-only this wave).
- Capabilities: `{ version: "0.3.0", tools: { qpdf, exiftool, c2patool }, scorers: { officialDetect: false } }` under empty ConfigProvider.

## Findings

BLOCKER: none

MAJOR: none

NIT: 256 MiB HTTP 400 is not hit; only the helper with `cap=1`. Invalid-base64 400 is HTTP-level.

NIT: No assertion that `POST /humanize` is absent. `catchAll` would map unknown routes to 400, not run Layer B.

NIT: Bearer 401/200 only exercised on `GET /health`. `HttpRouter.use(authorize)` wraps the router.

NIT: `capabilities.scorers.officialDetect` is `Option.isSome(ANTHROPIC_DETECT_URL)`. Wave 1 `Reporter` still hardcodes `OfficialUnavailable`. URL-set would advertise a scorer inspect/clean do not run. Implementer disclosed.

## Not done (correct)

OpenAPI, `anthropies serve`, skill HTTP path, Docker, CI. CLI version still `0.2.0`.
