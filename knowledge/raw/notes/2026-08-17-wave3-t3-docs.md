# Wave 3 Task 3 docs auditor note (Family 3 title-restoration copy)

Date: 2026-08-17
Role: docs auditor (hard gate)
Task: README/skill `humanize` names title restoration + residual risk; CLAIMS humanize row; no official-kill
Diff: `review-t3.diff`

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT (user-facing, not a CLAIMS lie).

## Locked surfaces (Wave 3 spec Family 3 + CLAIMS)

- README `### Humanize (title restoration)` names title restoration, residual statistical risk, print-prompt does not destamp, and that this is not an official-kill / does not prove the official Claude text detector will fail.
- Skill `humanize` is title restoration + residual risk + print-prompt does not destamp + not an official-kill. YAML still: `Does not claim official watermark removal.` Hard rule still: do not claim official-detector failure.
- `docs/CLAIMS.md` `## humanize` row states the allowed claim. CLI help sense matches `src/cli.ts:130`: title restoration, best-effort, refuses Claude and Gemini, print-prompt does not destamp.
- `destamp` stays forbidden as a capability. The denial `print-prompt does not destamp` is required (Wave 3 §2). Same pattern as the official-detector denial.
- Honesty box, manifesto, and `docs/legal/` unchanged (spec: they stay).

## Grep

Changed copy (README, SKILL, CLAIMS, CLI help, wiki ingest): no `undetectable`, `beats detector`, `official detector will fail` as a capability, `proves human-written`, `watermark removed` as a verdict. `official-kill` and `destamp` appear only as denials. Slash `commands/purge-anthropies.md` still denies official-detector failure (untouched).

## NIT

- `README.md:61` — "so the keyed text mark is no longer the shipped prose" is a shade stronger than the CLAIMS allowed claim (best-effort + residual risk). The next sentence names residual statistical risk and denies official-kill. Not a certificate.

## Out of this family

Family 4 full-diff trio / PR. Live model. 5-gram pass bar. `src/http/`. Honesty-box rewrite.
