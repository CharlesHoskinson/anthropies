# Task 1 STYLE auditor note (Family 0 honesty patch)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Honesty patch (Family 0) — markdown-only
Diff: `7a48c92..67597be` (`64e253b` honesty + `67597be` wiki ingest)
Scope: would these files force later TypeScript to be honest?

No TypeScript in this task. That is not a defect.

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## What STYLE is judging here

Plan persona is Effect/imports/`Fail≠Finding`/`@effect/cli`. Task 1 has none of that. The assigned STYLE question for Family 0 is whether the honesty-patch files would *force later TS to be honest*:

1. `docs/CLAIMS.md` is a copy contract Task 2+ can implement against (`honestyStanza`, Report fields, CLI help, demo last stanza).
2. No banned phrases on skill / slash / plugin.
3. No C2PA trigger restored on the skill.

If a later implementer follows `CLAIMS.md` + `CHANNELS.md`, they cannot honestly emit a mixed score, a `suspicious` field, a paraphrased official payload, a success banner, or a present-tense C2PA skill trigger.

## Contract check (would later `src/report.ts` / CLI be honest?)

### `docs/CLAIMS.md` — sufficient force

Matches spec §13 / §8 on the surfaces later TS will copy:

| Lock | Where | Later TS consumer |
|---|---|---|
| Forbidden phrases (incl. `destamp`, `undetectable`, `official detector will fail`, `proves human-written`, `clean C2PA from a Claude file`, `strip a Claude watermark`) | CLAIMS.md:11–21 | `official_claim_forbidden` test, CLI help, JSDoc |
| Forbidden Report fields (`suspicious`, `watermarkScore`, `watermark_score`, `destamped`, `undetectable`, `clean` as verdict, flat mixed `removed`) | CLAIMS.md:23 | `src/report.ts` Schema |
| Locked honesty stanza + three required denials (even on successful clean) | CLAIMS.md:27–42, 63–67 | `honestyStanza` → `Report.honesty` |
| Channel verbs; `removed` nested per channel | CLAIMS.md:44–61 | Detector adapters, Report encoder |
| Locked skill YAML (no C2PA until family tested; do not restore destamp triggers) | CLAIMS.md:71–81 | Task 5 skill trigger restore is narrow |
| Locked plugin description (curly apostrophe; “(once implemented)” C2PA) | CLAIMS.md:85–87 | package/plugin copy |
| CLI help exact sense (inspect never a single watermark score; clean does not remove keyed text mark; demo never claims official text-kill) | CLAIMS.md:89–100 | `@effect/cli` help in Task 2 |
| Report field meanings (`anyDeterministicHits` named so it cannot be read as “watermark detected”; `rewrite_metric` not a watermark score; `degraded` is Finding not Fail) | CLAIMS.md:102–110 | Schema + Fail≠Finding copy |
| Demo last stanza: honesty twice, four rows, no `✓ watermark cleared` | CLAIMS.md:112–114 | `demo` script |

Product sentence locked at CLAIMS.md:5. Header (CLAIMS.md:3) applies the contract to README, SKILL, plugin, slash, CLI help, Report, JSDoc, tests, fixtures, changelog, demo stdout — the same list as spec §13.

### `docs/CHANNELS.md` — fills the typed holes CLAIMS.md leaves

Later TS cannot treat channels as one number (CHANNELS.md:3). Certificate vs best-effort table is spec §7 (CHANNELS.md:9–14). Residual C2PA sentence after a hard strip is locked (CHANNELS.md:16).

Typed shapes later Task 2/4/5 will implement:

- `OfficialFinding = { _tag: "Unavailable" } | { _tag: "Available", url, raw }` (CHANNELS.md:34)
- Unset `ANTHROPIC_DETECT_URL` ⇒ `Unavailable`; no default URL; no `score` on `Unavailable` (CHANNELS.md:36)
- `rewrite_metric` object + `insufficient` if `n < 200` or `domain === "code"`; **no threshold in CI** (CHANNELS.md:40–51)
- `demo` C2PA track is `skipped` or `certified`, never implied from text (CHANNELS.md:30)
- Official never flips inspect to exit 1 (CHANNELS.md:36)
- `clean` does not remove the keyed text mark (CHANNELS.md:22)

Together these force `src/report.ts`, Detector, and demo copy to stay honest even if someone only reads docs/ and not the spec.

### Skill / slash / plugin — no banned phrase, no C2PA trigger

Skill YAML (SKILL.md:3–7) is the spec §13 block verbatim. Triggers are purge / Co-Authored-By / Generated-with banners / inspect vendor marks / slash names. No `strip a Claude watermark`. No `destamp SynthID-class text`. No `clean C2PA from a Claude file`. Denial line present: “Does not claim official watermark removal.”

Hard-rule rewrite SKILL.md:17: `Do not claim official-detector failure` (was `official detector will fail`). Step-7 grep stays clean without weakening the rule.

Python kept (`python3 -m anthropies` × clean / humanize print-prompt / humanize ollama / `--help`). Correct until Task 7.

Slash (commands/purge-anthropies.md:1–6): four brief sentences. No C2PA. No official-kill.

Plugin description (plugin.json:4) is the spec-locked sentence, including “(once implemented) hard-bound C2PA metadata” and “Does not defeat Anthropic’s unpublished text detector.” Version stays `0.1.0`. Not a present-tense C2PA capability.

Step-7 grep over `skills` `commands` `.claude-plugin` for
`destamp|undetectable|official detector will fail|proves human-written|strip a Claude watermark|clean C2PA from a Claude file`
has no matches on those roots (CLAIMS.md lists the phrases as *forbidden*; manifesto `undetectable` is Anthropic quotation + adjacent paraphrase, outside step-7 roots and outside this task’s file list except the deleted missing-brief sentence).

## What would *not* force later TS (and why it is not a BLOCKER)

1. **JSDoc / test / fixture identifier naming is not restated in CLAIMS.md.** Spec §13: test names `removesHardBoundC2paFromPng` never `removesWatermark`; fixtures `fixture-c2pa-present.png` never `sample_watermarked.*`. CLAIMS.md:3 says tests/JSDoc/fixtures are in scope but does not lock those names. Later STYLE on Task 2+ still has the spec and the plan’s auditor prefix. NIT, not a schema lie.

2. **CLI help `clean` names hard-bound C2PA as a thing clean does** (CLAIMS.md:93–94). That is spec §13 “exact sense,” not a Task 1 invention. CHANNELS.md:26–28 already separates skill/slash (no C2PA claim until the family lands) from plugin “(once implemented).” Task 2 should copy the locked help; Family 2 is when the capability becomes true.

3. **`honesty: string[]` is not typed in CLAIMS.md** (only “the locked stanza”). Spec §8 and Task 2 plan lock the array. Sufficient.

4. **Plugin keywords still `watermark` / `humanize`** (plugin.json:12). Brief said description only. Not a Report field. NIT.

5. **README `undetectable` at README.md:150** is outside Task 1’s file list. Do not fail Family 0 for it.

6. **Skill body “Target well under 50% surviving 5-grams”** (SKILL.md:54) is operational rewrite guidance, not a CI gate. CHANNELS.md:51 forbids a threshold in CI. Later TS that encodes `0.5` as pass/fail would violate CHANNELS, not this skill.

## Style / Effect notes (explicitly not defects)

- No `src/`, no imports, no `Effect.Service`, no `Fail` types, no tsconfig. Task 1 is Family 0 copy-first.
- No `any`, no `as unknown as`, no `node:fs`, no `process.env`. There is no TS to violate those rules.
- Plugin version `0.1.0` is correct until the package exists (Task 2 ships `0.2.0`).

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **docs/CLAIMS.md:3** — contract claims to cover JSDoc, tests, and fixtures, but never locks spec §13 identifier rules (`removesHardBoundC2paFromPng` / never `removesWatermark`; `fixture-c2pa-present.*` / never `sample_watermarked.*`). Later TS could introduce claim-shaped names while still matching the phrase list. Spec + Task 2 auditor protocol already catch this.

2. **.claude-plugin/plugin.json:12** — `keywords` still include `watermark` and `humanize`. Description-only change was in scope; keywords were not. Not a later Schema risk.

## Conclusion

CLAIMS.md + CHANNELS.md are strong enough that Task 2 `Report` / `honestyStanza` / OfficialFinding / rewrite_metric / CLI help / demo last stanza have an honest target. Skill has no C2PA trigger. Banned phrases are absent from skill/slash/plugin. APPROVE. Do not hold Family 1 for the NITs.
