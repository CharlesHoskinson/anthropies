# Wave 2 Task 2 STYLE auditor note (OpenAPI + serve)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: OpenAPI document and `anthropies serve` (Wave 2 family 2)
Diff: `1dab5ee..4213733` (`673e338` feat + `4213733` wiki ingest)
Scope: Effect HttpServer, `@effect/cli` serve, no raw `node:http`

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 2 / Wave 2 spec §2–§3)

Style fails on raw `node:http` in the HTTP layer, non-`@effect/cli` serve, Fail/Finding mix, `process.env`, or a non-platform HTTP stack.

| Check | Result | Cite |
|---|---|---|
| `@effect/platform` HttpServer | PASS | `src/http/server.ts:174-188` `HttpRouter.get("/openapi.json")` + `HttpServer.serve`. Tests still `NodeHttpServer.layerTest` (`tests/http-server.test.ts:11-14,66-81`). |
| `@effect/cli` serve | PASS | `src/cli.ts:148-176` `CliCommand.make("serve", { host, port })`. Defaults `defaultServeHost`/`defaultServePort` (`openapi.ts:4-7`). `Layer.launch(HttpApp.pipe(Layer.provide(NodeHttpServer.layer(createServer, { host, port }))))`. `CliCommand.run` version `0.3.0` (`cli.ts:178`). |
| no raw `node:http` | PASS | HTTP layer has zero `node:http`. `cli.ts:6,164` imports `createServer` only as the `NodeHttpServer.layer` server constructor. No `listen`, no `IncomingMessage`/`ServerResponse`. Spec §2: "HTTP layer uses `@effect/platform` HttpServer, not raw `node:http`." Same exception shape as `process.argv` at `CliCommand.run`. |
| Fail ≠ Finding | PASS | OpenAPI documents 400 Fail tags vs 200 Report. No `/humanize`. `officialDetect` described as presence bit, not a score. |
| Config not `process.env` | PASS | Unchanged `Config.option` / `Config.redacted`. No new env reads. |
| Forbidden imports | PASS | No `node:fs` / `fetch` / `process.env` / `console.log` / `Effect.runPromise` / `any` / `as unknown as` / default exports in new library TS. Tests use `node:url` (allowed) and platform `Command` for `--help`. Effect `^3.22.1`. Package `0.3.0`. |
| Honesty / no `/humanize` | PASS | `openapi.ts:89-211` paths are health/capabilities/openapi/inspect/clean only. Description: does not humanize; does not claim official-detector failure. `serve-cli.test.ts:24-30` help names loopback and rejects destamp copy. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **`eslint.config.js:65-71`** — `src/cli.ts` carve-out filters both `node:http` and unused bare `http`. The used import is only `node:http` `createServer` for `NodeHttpServer.layer`. Carve-out is wider than the bind adapter.

2. **`tests/http-server.test.ts:71-74`** — OpenAPI body is `as { openapi; paths }`, not `Schema.decodeUnknownSync`. Not the forbidden `as unknown as`. Shape check is enough for this family.

## Not defects

- `createServer` in `cli.ts` is the official `@effect/platform-node` bind factory. Plan "Forbidden: node:http" means no raw HTTP layer; spec §2 is the tighter wording. `HttpApp` stays platform-only.
- Static OpenAPI 3.0.3 instead of Effect `HttpApi`/`OpenAPISpec` 3.1.0. Spec locks 3.0.3.
- `/openapi.json` shares the bearer gate. Spec did not carve it out.
- README / `docs/CLAIMS.md` still 0.2.0 / no `serve` row. Out of this task's file list (DOCS).
- Carry-forward Task 1 nits (`DecodeError` remap, duck-typed `catchHttpFail`, unused `HealthResponse`) unchanged.

## Conclusion

Family 2 matches Wave 2 spec §2–§3 and the STYLE hard gate: platform HttpServer, `@effect/cli` serve, no raw `node:http` in the HTTP layer. APPROVE. Do not hold Task 3 for the NITs.
