# Wave 2 Task 5 STYLE auditor note (GitHub Actions CI)

Date: 2026-08-17
Role: STYLE (GitHub Actions CI surface / honesty / no-heavy-backends)
Task: GitHub CI for pnpm test and pnpm build (Wave 2 family 6 / plan Task 5)
Diff: `1c02563..8c5c396` (`8c5c396` feat + wiki ingest of implementer note)
Scope: `.github/workflows/ci.yml` plus this wiki ingest. Ignore later commit `f4e9828` (T3–T4 auditor notes). Do not re-litigate HTTP/Docker/skill.

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 5 / Wave 2 spec §2)

Style fails on a missing required CI surface (no workflow, wrong triggers, one OS missing, no `pnpm test` or `pnpm build`), an extra job that contradicts the locked spec (live-capture / official-detect / heavy-backend install), HTTP `/humanize`, destamp/official-kill/undetectable, or a version bump off `0.3.0`.

Nits do not fail the family.

| Check | Result | Cite |
|---|---|---|
| Workflow file at `.github/workflows/ci.yml` | PASS | New file, 35 lines. Only workflow under `.github/workflows/`. |
| Triggers: push and pull_request to `main` | PASS | `ci.yml:3-7` `on.push.branches: [main]` and `on.pull_request.branches: [main]`. |
| Matrix: ubuntu-latest AND windows-latest | PASS | `ci.yml:13-15` `strategy.matrix.os: [ubuntu-latest, windows-latest]`. `runs-on: ${{ matrix.os }}`. |
| `pnpm test` AND `pnpm build`; test before build | PASS | `ci.yml:31-35` Test then Build. |
| Node 22 | PASS | `ci.yml:23-26` `actions/setup-node@v4` `node-version: 22` + `cache: pnpm`. Matches Docker `node:22-bookworm-slim`. |
| pnpm pin matches Docker | PASS | `ci.yml:19-21` `pnpm/action-setup@v4` `version: 11.22.0`. Docker `corepack prepare pnpm@11.22.0 --activate`. |
| Frozen lockfile if lockfile exists | PASS | `ci.yml:28-29` `pnpm install --frozen-lockfile`. `pnpm-lock.yaml` is on the tree. |
| No heavy backends on runners | PASS | No apt / choco / winget. No qpdf / exiftool / c2patool / MarkLLM / pixel. |
| No live-capture / official-detect job | PASS | Single job `ci`. Steps: checkout, pnpm setup, node setup, install, test, build. No `pnpm test:live`. |
| No `/humanize`; honesty; version 0.3.0 | PASS | Diff does not add a route. No destamp / official-kill / undetectable claim. `package.json` stays `0.3.0` (untouched). |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **`ci.yml:17,19,23`** — `actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4` are floating major tags, not SHA-pinned. Same class as Task 4's unpinned `node:22-bookworm-slim` digest. Reproducible enough for this wave; not a spec fail.

2. **`ci.yml:10-16`** — job has no `permissions:` (least-privilege `contents: read`) and no `timeout-minutes`. Extra hardening, not required by §2.

## Not defects

- Workflow has never run on GitHub. First push/PR to `main` is the real execution check. That is QA residual, not a missing CI surface.
- Windows matrix is unproven here. Tests look path-safe; a Windows-only fail will show on the first Actions run. STYLE cannot fail a run that has not happened.
- No lint job. Implementer almost added one and did not. Spec is `pnpm test` + `pnpm build`.
- No `fail-fast: false`. Default fail-fast is fine; both OSes are still in the matrix.
- `package.json` has no `packageManager` field. Workflow + Docker both pin `11.22.0`. Consistent.
- `docs/CLAIMS.md` still has no `serve` row. Out of this task's file list (DOCS / later).
- HTTP / Docker / skill families are prior. Not re-litigated.
- Commit `f4e9828` (T3–T4 auditor notes) is after this family. Ignored.

## Conclusion

Family 6 matches Wave 2 spec §2 and the STYLE hard gate: GitHub Actions `pnpm test` + `pnpm build` on push/PR to `main`, ubuntu + windows, Node 22, pnpm 11.22.0, frozen lockfile, no heavy backends, no live-capture, no `/humanize`. APPROVE. Do not hold QA/DOCS for the NITs.
