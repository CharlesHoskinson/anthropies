# Task 7 QA auditor note (capture, demo, delete Python)

Date: 2026-08-17
Role: QA
Task: Capture, demo, delete Python (Family 6)
Verdict: APPROVE
Diff: `cdec59b..4863f80` (`1cc6a52` feat + `4863f80` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| TDD evidence (RED then GREEN) | PASS |
| `premark_unknown_model` | PASS |
| `official_unavailable_default` | PASS |
| live skip (`live_capture_smoke`) | PASS |
| Empty allowlist | PASS |
| Demo script not a gold test | PASS |
| `--json` purity | PASS |
| No official-detect lie | PASS |
| Python package deleted | PASS |

## TDD

RED documented as missing `../src/services/capturer.js` on `premark-model.test.ts`. `official_unavailable_default` was already green (Inspector always emits `official: unavailable`). GREEN: 38 tests / 16 files; `pnpm build`; `node dist/cli.js --version` → `0.2.0`. Feat commit `1cc6a52`.

## Surfaces

- `tests/premark-model.test.ts` `premark_unknown_model`: `markedModelIds` is `[]`; `Capturer.capture({ model: "claude-opus-5", allowlist: [] })` is `PreMarkModel` before HTTP/key. `allowlist.json` decodes as `[]` and equals `markedModelIds`. Sidecar encode has no `watermarked` / `sampled` / `sk-ant`.
- `tests/official-unavailable.test.ts` `official_unavailable_default`: inspect owned text → official findings all `unavailable`; `JSON.stringify(report)` has no `"score"`; `OfficialFinding` / `Report` reject an injected score.
- `tests/live-capture.test.ts` `live_capture_smoke`: returns before HTTP unless `LIVE=1` and key and `markedModelIds[0]`. Empty allowlist is the Wave-1 skip. When it runs: sidecar + official unavailable; no mark/unmark assert.
- Empty allowlist: `src/models/allowlist.json` is `[]`; `markedModelIds` is `[]`. Exact ID match only.
- Demo is `pnpm demo` (`node dist/cli.js demo`), not `pnpm test`. `demo_honesty` is not a CI test. `official_claim_forbidden` only locks channel-table copy.
- `--json`: `Reporter.print` still `Console.error` honesty then `Console.log(JSON.stringify(Schema.encodeUnknownSync(Report)))`. Capture/demo use `Console.error` only. Default `pnpm test` does not construct `HttpClient`. `json_stdout_purity` unchanged.
- Official: reports still `OfficialUnavailable`; honesty `unavailable (ANTHROPIC_DETECT_URL unset)` plus does-not-prove official detector. No score field. Channel table is four statuses, not a mixed score.
- Python: `src/anthropies/`, `pyproject.toml`, `tests/test_clean.py`, `tests/test_humanize.py` gone. Skill/commands/README have no `python3 -m anthropies`.

## Findings

BLOCKER: none

MAJOR: none on Task 7 named tests.

NIT: TDD RED is missing-module only. `official_unavailable_default` was GREEN-only this task.

NIT: `premark_unknown_model` locks the Fail tag, not CLI exit 2. Teardown maps `PreMarkModel` → 2 (`src/cli.ts:42-50,186-188`). Report ran the subprocess.

NIT: Live `Effect.catchAll` does not catch `orDie` defects. Report overclaims HTTP skip. Wave-1 empty allowlist never reaches HTTP.

NIT: `pnpm demo` provides `NodeHttpClient.layer` even when capture is skipped.

## Not done (correct)

HTTP service, Docker, MarkLLM, pixel. Non-empty allowlist. Command stays `capture`. Version stays `0.2.0`. Demo is not a CI gate.
