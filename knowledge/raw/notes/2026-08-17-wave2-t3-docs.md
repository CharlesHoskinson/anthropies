# Wave 2 Task 3 docs auditor note (Skill HTTP path)

Date: 2026-08-17
Role: docs auditor
Task: Skill HTTP client path (Wave 2 family 3)
Verdict: APPROVE
Diff: `0524caa..9c05bb9` (`216e961` docs + `9c05bb9` wiki ingest)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT (`docs/CLAIMS.md` still has no `serve` row; out of this file list).

## Required checks

| Check | Result |
|---|---|
| Product sentence | PASS — SKILL + README keep “Restore clean title in Outputs the user already owns.” |
| Version `0.3.0` | PASS — README how-to-run is `0.3.0` (no leftover `0.2.0`). SKILL health expected `{"ok":true,"version":"0.3.0"}`. |
| `serve` | PASS — README adds `npx anthropies serve`; loopback `127.0.0.1:8765`; `GET /health`, `POST /inspect`, `POST /clean`. SKILL names `anthropies serve` / `node dist/cli.js serve` as the FileSystem owner. |
| Skill HTTP | PASS — `ANTHROPIES_SERVICE_URL` default `http://127.0.0.1:8765`. Health `curl -sS -f` first. Fail: say so; do not invent success. Operator-required → stop. Else `npx anthropies` / `node dist/cli.js`. |
| Base64 contract | PASS — `POST /inspect` `/clean` `{ file, name }`; `cleaned` is base64; Layer A / hard-bound metadata only. |
| No `/humanize` | PASS — SKILL: “There is no HTTP `/humanize`.” Humanize is CLI-only. README same. |
| CLAIMS / no official-kill | PASS — YAML: “Does not claim official watermark removal.” Required denial at SKILL:68. Official `unavailable` unless `ANTHROPIC_DETECT_URL`. Residual is not a certificate. No destamp / undetectable / watermark-removed verdict / mixed score. Capture is not called a sample. |

## Locked surfaces (this task)

- `skills/purge-anthropies/SKILL.md`: HTTP-first inspect/clean; health first; npx fallback; no `/humanize`.
- `README.md`: version `0.3.0` + `serve` paragraph.

## Grep

Diff + SKILL + README serve block: no `destamp`, no `undetectable`, no `watermark removed` as a verdict, no `strip a Claude watermark`. Denial “does not prove the official Claude text detector will fail” is the locked CLAIMS line, not a capability.

## NIT

- `docs/CLAIMS.md` CLI help list still has no `serve` row. Task 2 already reserved this. File list is SKILL + README.

## Not this task

Docker / compose (Task 4). CI (Task 5). POSIX `base64`/`basename` is host-portability, not a CLAIMS defect.
