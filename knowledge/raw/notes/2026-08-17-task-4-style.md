# Task 4 STYLE auditor note (humanize print-prompt + origin blocklist)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Humanize print-prompt + origin blocklist (Family 1)
Diff: `55e669b..6c3f73f` (`7781e0a` feat + `6c3f73f` wiki ingest)
Scope: originBlocked purity, Humanizer Effect.Service, no HttpClient in default path, OriginBlocked TaggedError

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 4 / spec §5 / §9 / §11.3)

Style fails on impure `originBlocked`, Humanizer not `Effect.Service`, `HttpClient` on the default print-prompt path, `OriginBlocked` not a `Schema.TaggedError`, forbidden imports, shared AppR, Fail/Finding mix, or non-`@effect/cli` argv.

| Check | Result | Cite |
|---|---|---|
| originBlocked is pure | PASS | `src/services/humanizer.ts:85-89` `(backend, model) => boolean`. Token list is a const. `toLowerCase` + `includes`. Not an Effect. |
| Humanizer Effect.Service + Default | PASS | `humanizer.ts:162-212` `Effect.Service` + `accessors: true` + `Default`. `dependencies: [Reporter.Default]`. No typed AppR. CLI merges `Humanizer.Default` (`src/cli.ts:146-151`). |
| no HttpClient in default path | PASS | Grep of `src/` and `tests/` TS: no `HttpClient`. `rewrite` (`humanizer.ts:144-159`) is print-prompt only. ollama / openai-compatible note fallback; they do not construct HTTP. Default tests provide `Humanizer.Default` + `NodeContext.layer`. |
| Humanizer R is FileSystem | PASS | `humanize` typed `Effect<HumanizeResult, OriginBlocked, FileSystem>` (`humanizer.ts:170`). `humanizeFile` same + Fail union (`humanizer.ts:174-177`). Matches brief and spec §5.2 print-prompt row. |
| OriginBlocked TaggedError | PASS | `src/fail.ts:10-13` `Schema.TaggedError`. CLI `failTags` + teardown exit 2 (`src/cli.ts:40-48,179-181`). Origin check runs before `writeAtomic` (`humanizer.ts:180-183`). |
| Fail ≠ Finding | PASS | `OriginBlocked` is a Fail. Statistical stays a Finding (`best-effort`) on `Report` via `reportFromHumanize` (`humanizer.ts:110-135`). `rewrite_metric.status = "not-run"`. |
| Config not process.env | PASS | `readRewriteTarget` (`humanizer.ts:137-142`) yields `rewriteBackend` / `rewriteModel` from `src/config.ts`. `orDie` keeps the error channel `OriginBlocked`. |
| Layer A stays pure | PASS | `applyLayerA` still `string → LayerAResult` (`src/layer-a.ts:110`). Humanizer calls it; does not wrap it as Effect. |
| rewrite-metric is pure | PASS | `notRunMetric` / `computeRewriteMetric` / `unicodeWords` (`src/rewrite-metric.ts`) are sync. Print-prompt uses `notRunMetric` only. |
| no node:fs | PASS | Writes via `Reporter.writeAtomic` + platform `FileSystem`. Tests use platform FS + `NodeContext`. |
| @effect/cli | PASS | `humanize` is `CliCommand.make` + Options/Args (`src/cli.ts:98-127`). `process.argv` only at `run`. |
| Forbidden imports | PASS | No `node:fs`, `fetch`, `process.env`, `console.log`, `Effect.runPromise`, `any`, `as unknown as`, default exports in library/tests TS. |
| 0.2.0 / Effect 3 | PASS | `package.json:3,26` `0.2.0` / `effect@^3.22.1`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **spec §5.1 Test layers** — `Humanizer` exposes `Default` only. Tests provide Live + `NodeContext.layer`. Same residual as Task 3. Fine for Family 1 print-prompt.

2. **`Effect.orDie` + `Config.literal`** — `readRewriteTarget` (`humanizer.ts:139-140`) dies on Config error. Backend `claude` cannot enter `originBlocked` because `ANTHROPIES_REWRITE_BACKEND` is `Config.literal("print-prompt", "ollama", "openai-compatible")` (`src/config.ts:3-15`). Spec §9.1 says `OriginBlocked` when backend/model contains origin tokens. The model string (`claude-opus`, `gemini-2.5`, `synthid-demo`) is the live Fail path; a typed backend defect exits 1, not 2.

## Not defects

- `humanize` advertises `FileSystem` in `R` while `rewrite` itself is `R = never`. Brief requires that signature. `humanizeFile` is the FS consumer.
- ollama / openai-compatible HTTP not implemented. Brief: stubs optional. Keeping `HttpClient` out of `Humanizer.Default` is the STYLE-correct default path.
- `computeRewriteMetric` is unused by print-prompt. Spec: print-prompt `status = not-run`. Function is pure and ready for a later backend.
- `ANTHROPIES_REWRITE_BACKEND=claude` as Config defect is a Task 2 lock, not a Task 4 Fail/Finding mix.
- Python `src/anthropies/` stays (urllib HTTP). STYLE gate is TS `src/` + `tests/`.
- `ResidualHits` remains CLI-edge exit 1. `OriginBlocked` is exit 2, no write.

## Conclusion

Print-prompt Humanizer matches spec §5 / §9 / §11.3 and the Task 4 STYLE hard gate: `originBlocked` is pure, `Humanizer` is `Effect.Service`, default path has no `HttpClient`, `OriginBlocked` is a `TaggedError`. APPROVE. Do not hold Family 1 for the NITs.
