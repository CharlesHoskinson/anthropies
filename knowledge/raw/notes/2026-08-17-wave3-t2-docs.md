# Wave 3 Task 2 docs auditor note (Family 2 stronger prompts)

Date: 2026-08-17
Role: docs auditor (hard gate)
Task: Stronger Layer B prompts (clause-order / H-gram break; keep facts/URLs/fences)
Diff: `d9a9873..HEAD` (`review-d9a9873..HEAD.diff`)

## Verdict

APPROVE. 0 BLOCKER. 0 MAJOR. 1 NIT (implementer wiki, not user-facing).

## Locked surfaces (Wave 3 spec Family 2 + CLAIMS)

- `src/services/humanizer.ts` `PROSE_PROMPT` / `CODE_PROMPT` require clause-order, sentence-boundary, and discourse-marker / function-word change (H-gram break). Facts, numbers, names, URLs, citations, and code fences stay byte-stable. Code still refuses public-API / protocol / snapshot / import / behavior edits.
- Prompts are rewrite instructions, not certificates. No `undetectable`, `beats detector`, destamp-as-capability, or official-kill.
- print-prompt note unchanged: `print-prompt: does not destamp; run this on an unmarked local model`. `rewrite_metric.status = not-run`.
- CLI `humanize` help still CLAIMS sense (best-effort; refuses Claude and Gemini). Title-restoration product copy is Family 3.
- `docs/CLAIMS.md` unchanged. Forbidden phrases stay forbidden.

## Grep

Changed copy (`humanizer.ts` prompts, `humanize-print-prompt.test.ts`, wiki ingest): no `undetectable`, `beats detector`, `official detector will fail`, `proves human-written`, `watermark removed` as a verdict. `destamp` appears only in the spec-mandated denial and notes describing that denial.

## NIT

- Implementer synthesis says H-grams "do not survive." The prompt says "almost no" original 5-word sequence survives. Wiki-only; not a user-facing CLAIMS lie.

## Out of this family

README / skill title-restoration wording (Family 3). Live model. 5-gram pass bar. `src/http/`.
