# Wave 2 Task 4 QA auditor note (Docker + compose)

Date: 2026-08-17
Role: QA
Task: Docker image and loopback compose (Wave 2 family 5 / Task 4)
Verdict: APPROVE
Diff: `9c05bb9..1c02563` (`c00f2c8` feat + `1c02563` wiki ingest)

## Checks (this persona)

| Check | Result |
|---|---|
| CMD serve | PASS |
| compose `127.0.0.1:8765:8765` | PASS |
| No heavy backends | PASS |
| One core image + 8765 | PASS |
| Read-only root + tmpfs | PASS |

## Surfaces

- `Dockerfile` runtime `CMD ["node", "dist/cli.js", "serve", "--host", "0.0.0.0", "--port", "8765"]`. Matches plan Task 4. In-container bind is `0.0.0.0`; host publish is loopback.
- `compose.yaml` ports: `"127.0.0.1:8765:8765"`. No `command:` override — inherits CMD serve. Image `anthropies:0.3.0`. Single service.
- No `apt-get` / no qpdf / exiftool / c2patool in the image. `node:22-bookworm-slim` only. `/capabilities` will report those tools absent (locked "no heavy backends", not silent success).
- Design §2 extras present: `read_only: true`, `tmpfs: /tmp`. HTTP uploads use `makeTempDirectoryScoped` (`src/http/server.ts`). Official stays unavailable unless `ANTHROPIC_DETECT_URL` is set (commented, unset). No HTTP `/humanize`.
- `.dockerignore` drops `node_modules`, `dist`, `.git`, `.worktrees`, `knowledge`, tests, docs, fixtures, skills. Build copies `package.json` / lock / workspace / `tsconfig` / `src` only.
- README: `docker compose up --build` publishes `127.0.0.1:8765:8765`; process binds `0.0.0.0:8765`; no heavy backends. Version stays `0.3.0`.

## Findings

BLOCKER: none

MAJOR: none

NIT: no in-tree compose/image smoke test. Task 4 file list is `Dockerfile` / `compose.yaml` / `.dockerignore` only. CI is Task 5.

NIT: `pnpm prune --prod` runtime shrink not proven (implementer disclosed).

## Not done (correct)

GitHub CI (Task 5). Installing qpdf/exiftool/c2patool. Publishing `8765:8765` on all host interfaces. HTTP `/humanize`. Official-kill / destamp claims.
