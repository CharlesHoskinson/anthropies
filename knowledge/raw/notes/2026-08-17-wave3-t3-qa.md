# Wave 3 Task 3 QA auditor note (Family 3 title-restoration copy)

Date: 2026-08-17
Role: QA
Task: README/skill humanize section names title restoration + residual risk
Verdict: APPROVE
Diff: worktree `feat-wave3-layerb` Family 3 copy (`review-t3.diff` not on this host)
Scope: title-restoration copy; residual risk; print-prompt does not destamp; no official-kill

## Checks (this persona)

| Check | Result |
|---|---|
| README `### Humanize (title restoration)` names title restoration + residual risk | PASS |
| Skill names title restoration + residual risk; YAML still denies official watermark removal | PASS |
| CLAIMS `humanize` row states allowed claim; destamp-as-capability still forbidden | PASS |
| CLI `humanize` help: title restoration; print-prompt does not destamp | PASS |
| print-prompt still `rewrite_metric.status = not-run` (does not destamp) | PASS |
| No official-kill / watermark-removed / undetectable as a verdict | PASS |
| Honesty box / manifesto / legal stay | PASS |

## Independent review

Static review of Family 3 copy on `feat-wave3-layerb`. Tests not executed in this auditor environment.

README How-to-run adds `### Humanize (title restoration)`: best-effort non-origin rewrite, residual statistical risk, not an official-kill, does not prove the official Claude text detector will fail. Default print-prompt **does not destamp**. Skill lead + procedure + report residual line match. YAML still `Does not claim official watermark removal`.

`docs/CLAIMS.md` adds a `humanize` row: title restoration, residual risk, print-prompt does not destamp, not an official-kill or official-detector certificate. `destamp` stays forbidden as a capability; the denial is required (Wave 3 §2). CLI help at `src/cli.ts:130` matches CLAIMS sense.

Behavior still matches the denial: `printPromptNote` is `print-prompt: does not destamp; run this on an unmarked local model`. `humanize_print_prompt_default` and structure tests keep `rewrite_metric.status = not-run`. Prompts have no destamp-as-capability / official-kill. Honesty box, manifesto, and `docs/legal/` unchanged. No `src/http/`. No 5-gram CI bar.

## Surfaces

- `README.md` — `### Humanize (title restoration)`
- `skills/purge-anthropies/SKILL.md` — title restoration + destamp denial + residual risk
- `docs/CLAIMS.md` — `humanize` row + CLI help sense
- `src/cli.ts` — `humanize` description
- `src/services/humanizer.ts` — `printPromptNote`; print-prompt still not-run
- `tests/humanize-print-prompt.test.ts` — default not-run; no `watermark removed`

## Findings

BLOCKER: none

MAJOR: none

NIT: `humanize_print_prompt_default` matches `/print-prompt/` on the note, not `/does not destamp/`. Product note already carries the denial.

NIT: README How-to-run still says Version is `0.2.0` while `package.json` is `0.4.0`. Not this family’s gate.

NIT: Skill in-session rewrite says “Target well under 50% surviving 5-grams.” Procedure hint, not a CI pass bar.

## Not done (correct)

No destamp-as-capability. No official detect client. No Family 4 full-diff / PR. No `src/http/`. Honesty box stays.
