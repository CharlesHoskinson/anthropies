# Task 6 STYLE auditor note (markup + office + PDF)

Date: 2026-08-17
Role: STYLE (TypeScript/Effect style)
Task: Markup + office + PDF (Families 3–5)
Diff: `e2865eb..5091d86` (`5904842` feat + `5091d86` wiki ingest)
Scope: no node:fs, zip in-memory, ProcCommand.make shell:false, Fail≠Finding (missing qpdf is Finding not Fail), Effect.Service, FileSystem R

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 6 / spec §5 / §6 / §9 / §12)

Style fails on forbidden imports, shared AppR, Fail/Finding mix, non-`@effect/cli` argv, PdfTools not `Effect.Service`, `node:fs`, zip on disk, `shell: true` / `runInShell`, HttpClient on the default path, or missing-qpdf as a Fail.

| Check | Result | Cite |
|---|---|---|
| PdfTools Effect.Service + Default | PASS | `src/formats/pdf.ts:57-112` `Effect.Service` + `accessors: true` + `succeed`. Wired from Inspector/Cleaner (`inspector.ts:163`, `cleaner.ts:178`). No typed AppR. |
| ProcCommand.make, never shell | PASS | `ProcCommand.make(bin, flag)` / `make("exiftool", …)` / `make("qpdf", "--linearize", "--", …)` (`pdf.ts:41,88,91`). No `runInShell`, no `shell: true`. `@effect/platform` `Command.make` is argv spawn. |
| Missing qpdf/exiftool is Finding | PASS | `toolPresent` catchAll → false (`pdf.ts:40-44`). Missing → `{ degraded: true }` (`pdf.ts:78-79`), then `makeContainerReport` Finding `c2pa: degraded` (`reporter.ts:92-103`). Never `DecodeError`. |
| Zip in-memory, cap before inflate | PASS | `fflate` `unzipSync`/`zipSync` on `Uint8Array` (`zip.ts:1,80-100`). CD sizes checked first (`zip.ts:73-79`). Over cap / lying CD → `ZipFail` → `DecodeError` (`inspector.ts:102-103`, `cleaner.ts:99-100`). |
| no node:fs | PASS | Load/write via platform `FileSystem` (`registry.ts:99-109`, `pdf.ts:81-100`, `reporter.ts:187-203`). Tests: `FileSystem` + `NodeContext`. No `node:fs` / `fs/promises` / `node:child_process`. |
| FileSystem R (CommandExecutor only on PDF) | PASS | svg/html/md/docx/odt handlers are pure bytes (`R = never`). PDF inspect/strip need `CommandExecutor` + `FileSystem` temps. Spec §5.2. No `HttpClient`. |
| Fail ≠ Finding | PASS | Zip bomb / undecodable zip is `DecodeError` (`fail.ts:27-31`). Missing tools are Findings + `degraded: true`. Residual is CLI exit 1 unless degraded (`report.ts:96-103`). |
| ESM / strict / forbidden imports | PASS | No `any`, `as unknown as`, `process.env`, `console.log`, `Effect.runPromise`, default exports in library/tests TS. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **spec §5.1 Test layers** — `PdfTools` exposes `Default` only (`succeed` with Live `ProcCommand.make`). Tests build `Layer.succeed(PdfTools, PdfTools.of(…))` (`tests/cert-pdf.test.ts:17-34`) rather than a first-class `Test`. Same residual as Tasks 3–5. Live missing-tool path still degrades (catchAll), so Fail≠Finding does not depend on the double replacing a sealed Default.

2. **PDF temps** — strip writes `${path}.anthropies-pdf-tmp` / `.anthropies-pdf-lin` via `FileSystem` (`pdf.ts:82-100`), not a scoped temp. Not `node:fs`. Not the user path handed to the binary. Prefer `makeTempFileScoped` later.

## Not defects

- `Command.make` has no `{ shell: false }` options bag. Spec §6 / §12 “shell: false” is satisfied by `make` (StandardCommand) and the absence of `runInShell`.
- After `toolPresent` is true, spawn/IO errors map to `DecodeError` (`pdf.ts:84-96`). That is I/O Fail, not MissingTool. Non-zero exit after spawn is still Finding + `degraded` (`pdf.ts:101-107`).
- Inspector/Cleaner if-chains instead of `Match.valueTags`. Same byte-family pattern as raster (Task 5).
- One feat commit for three families is process, not STYLE.
- Python `src/anthropies/` stays. STYLE gate is TS `src/` + `tests/`.

## Conclusion

Task 6 matches spec §5 / §6 / §9 / §12 and the STYLE hard gate: no `node:fs`, in-memory zip, `ProcCommand.make` without shell, missing qpdf is a Finding, `PdfTools` is `Effect.Service`, FileSystem R. APPROVE.
