# Wave 3 Task 2 QA auditor note (Family 2 stronger prompts)

Date: 2026-08-17
Role: QA
Task: Stronger Layer B prompts (clause-order / H-gram break; keep facts/URLs/fences)
Verdict: APPROVE
Diff: `d9a9873..HEAD` (`review-d9a9873..HEAD.diff`)
Scope: print-prompt structure-rule snapshots; no official-kill in prompts

## Checks (this persona)

| Check | Result |
|---|---|
| PROSE_PROMPT requires clause-order / sentence-boundary / discourse-marker / H-gram break | PASS |
| CODE_PROMPT requires the same in comments/docstrings | PASS |
| Facts / URLs / fences stay byte-stable | PASS |
| print-prompt snapshot contains those structure rules | PASS |
| No official-kill / undetectable / beats-detector in prompts | PASS |
| print-prompt still `rewrite_metric.status = not-run` | PASS |
| Code prompt still refuses public-API / behavior edits | PASS |

## Independent review

Static review of `d9a9873..HEAD`. Tests not executed in this auditor environment.

`PROSE_PROMPT` and `CODE_PROMPT` are no longer a soft “Rules:” list. Both name H-grams and require clause order, sentence boundaries, and discourse markers / function words as separate bullets, plus no in-place synonym-swap. Facts, numbers, names, URLs, citations, and code fences stay byte-stable. CODE_PROMPT still forbids public-API / protocol / snapshot / import / behavior edits.

`humanize_prompt_structure_rules` in `tests/humanize-print-prompt.test.ts` runs `Humanizer.humanize` for prose and code. Emitted text must `startsWith` the exported prompt, contain the input, match `/clause order|sentence boundar|discourse marker|5-word sequence|H-gram|fact|URL|fence/i`, and must not match `/undetectable|beats detector/i`. Metric stays `not-run`. Existing `humanize_print_prompt_default` still forbids `watermark removed`.

Prompts have no destamp / official-kill / detector-fail copy. The print-prompt note still says `does not destamp` (honesty, not a kill). `completeRewrite` still receives `promptFor(kind) + cleaned`. No `src/http/`. No 5-gram pass bar.

## Surfaces

- `src/services/humanizer.ts` — `PROSE_PROMPT` / `CODE_PROMPT`; `promptFor` used by print-prompt and HTTP rewrite.
- `tests/humanize-print-prompt.test.ts` — `humanize_prompt_structure_rules` prose + code.

## Findings

BLOCKER: none

MAJOR: none

NIT: structure tests are regex contains, not vitest `.toMatchSnapshot()` files. Spec asked for tests that print-prompt text contains the structure rules; these do.

NIT (carry-forward): print-prompt tests still do not pin `ConfigProvider`. A developer env with `ANTHROPIES_REWRITE_BACKEND=ollama` can leave the default-CI path.

## Not done (correct)

No live model. No destamp claim. No Family 3 README / skill title-restoration copy. No 5-gram gate. No `src/http/`.
