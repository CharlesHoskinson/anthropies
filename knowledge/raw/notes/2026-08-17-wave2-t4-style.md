# Wave 2 Task 4 STYLE auditor note (Docker + compose)

Date: 2026-08-17
Role: STYLE (Dockerfile / compose loopback / no heavy image)
Task: One core Docker image and loopback compose (Wave 2 family 4)
Diff: `9c05bb9..1c02563` (`c00f2c8` feat + `1c02563` wiki ingest)
Scope: `Dockerfile`, `compose.yaml`, `.dockerignore`, README Docker paragraph. No TypeScript in this family.

## Verdict

**APPROVE**

- BLOCKER: 0
- MAJOR: 0
- NIT: 2

## STYLE gate (plan: Task 4 / Wave 2 spec §2)

Style fails on a LAN publish (`8765:8765` / `0.0.0.0:8765` on the host), a heavy image (qpdf / exiftool / c2patool / MarkLLM / pixel), HTTP `/humanize`, destamp/official-kill, or a second service.

| Check | Result | Cite |
|---|---|---|
| One core image; Node 22 slim | PASS | `Dockerfile:4,18` `node:22-bookworm-slim` build + runtime. No second service. `pnpm prune --prod` then copy `package.json` / `node_modules` / `dist` only (`Dockerfile:16,23-25`). |
| In-container bind `0.0.0.0:8765` | PASS | `Dockerfile:33` `CMD ["node", "dist/cli.js", "serve", "--host", "0.0.0.0", "--port", "8765"]`. Plan Task 4 checkbox. |
| Host publish loopback | PASS | `compose.yaml:9-10` `"127.0.0.1:8765:8765"`. Comments + README: process bind is in-namespace; host mapping stays loopback. Not `8765:8765`. |
| No heavy backends / no heavy image | PASS | No `apt-get`, no qpdf / exiftool / c2patool. Comments on Dockerfile + compose say those tools stay absent. Runtime is slim Node + prod modules. |
| read-only root + tmpfs | PASS | `compose.yaml:11-13` `read_only: true` + `tmpfs: /tmp`. Spec §2 "if easy". |
| No `/humanize`; official unavailable | PASS | Compose comment: no HTTP `/humanize`. `ANTHROPIC_DETECT_URL` commented, unset. README Docker paragraph same. |
| Version / non-root | PASS | Image `anthropies:0.3.0` (`compose.yaml:8`). `USER node` (`Dockerfile:27`). `init: true`. HEALTHCHECK hits `http://127.0.0.1:8765/health`. |
| Context small | PASS | `.dockerignore` drops `.git`, `.worktrees`, `node_modules`, `knowledge`, tests, docs, fixtures, skills, `*.md`. |

## Findings

### BLOCKER

None.

### MAJOR

None.

### NIT

1. **`Dockerfile:4,18`** — `node:22-bookworm-slim` is an unpinned floating tag (no digest). Slim is the right class. Pin is reproducibility, not a spec fail.

2. **`compose.yaml:11-18`** — `read_only` + `/tmp` tmpfs landed. No `cap_drop: [ALL]` / `no-new-privileges`. Extra hardening, not required by §2.

## Not defects

- `--host 0.0.0.0` in `CMD` is the in-container bind. Compose publish list is what keeps it off the LAN.
- bookworm-slim vs alpine: spec is "no heavy backends", not alpine.
- HEALTHCHECK uses Node `fetch` (no curl/wget in slim). Loopback inside the namespace is correct.
- `docs/CLAIMS.md` still has no `serve` row. Out of this task's file list (DOCS / later).
- CI is Task 5. No workflow in this diff is correct.

## Conclusion

Family 4 matches Wave 2 spec §2 and the STYLE hard gate: loopback compose, one slim core image, no heavy backends. APPROVE. Do not hold Task 5 for the NITs.
